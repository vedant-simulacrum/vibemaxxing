# auth conformance suite

This directory defines the future auth conformance fixtures. README files do not count as executable evidence; the eval must remain not_applicable until real fixtures and runners are added.
