# `privacy` conformance suite

Case prefix: `PV`. Subjects: `rust`. Harness contract: `docs/verification/CONFORMANCE_HARNESS.md`.

One case over the ten boundary canaries. The positive and reject-before-egress pairing is checked today by a validator stage; nothing drives the boundaries themselves, because none of them exist.
