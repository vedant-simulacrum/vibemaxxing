# `evidence` conformance suite

Case prefix: `EV`. Subjects: `rust`, `go`. Harness contract: `docs/verification/CONFORMANCE_HARNESS.md`.

Eleven cases over the source receipt, the appraisal result and the appraisal policy bundle. No eval suite in `evals/suites/suites.yaml` names this directory, so a result document for it has never existed.
