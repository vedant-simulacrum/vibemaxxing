# `onboarding` conformance suite

Case prefix: `ON`. Subjects: `typescript`. Harness contract: `docs/verification/CONFORMANCE_HARNESS.md`.

One case over a study template whose `status` is `not_run` and whose participant count is zero. The suite has no negative case and no study result; `onboarding-trust` waits on `conformance/onboarding/study-results-v1.json`, which does not exist.
