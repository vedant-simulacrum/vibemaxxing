# `adapters` conformance suite

Case prefix: `AD`. Subjects: `rust`. Harness contract: `docs/verification/CONFORMANCE_HARNESS.md`.

Seven cases covering the adapter-one receive surface and the two-stage identity strip. No runner exists; the identity-strip cases are executed today as a validator stage that emits no result document and cannot report a divergence class.
