# `models` conformance suite

Case prefix: `MD`. Subjects: `python`. Harness contract: `docs/verification/CONFORMANCE_HARNESS.md`.

Three cases over the T20 registry and its optimization evidence. The registry records zero selection runs and zero certified slots, and no eval suite consumes the directory.
