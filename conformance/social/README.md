# `social` conformance suite

Case prefix: `SO`. Subjects: `go`. Harness contract: `docs/verification/CONFORMANCE_HARNESS.md`.

One case over ten scenario names. A scenario name is a plan for a case and is not a case: nothing in the file states an input, an expectation or a reason code, so the suite has no executable case and therefore no negative case either.
