# `pricing` conformance suite

Case prefix: `PC`. Subjects: `go`. Harness contract: `docs/verification/CONFORMANCE_HARNESS.md`.

One case over a deliberately synthetic test-only dataset. The suite has no negative case and no signature; `pricing-provenance` waits on `conformance/pricing/pricing-v1-signature.json`, which does not exist.
