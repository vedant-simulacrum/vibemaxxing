# Decisions D-200 through D-288

<!-- generated: decision-traceability -->

| ID | Normative owner | Implementation owner/work units | Machine/state ownership | Platform scope | Required executable evidence |
|---|---|---|---|---|---|
| D-200 | Not implementation-bearing: sets the required fields for work-unit entries in the planning breakdown | — | — | None | — |
| D-201 | Not implementation-bearing: defines how work-unit status is recorded and checked against the tree | — | — | None | — |
| D-202 | Not implementation-bearing: requires every planning-schema table to be named by a work unit, a planning-coverage rule | — | — | None | — |
| D-203 | Not implementation-bearing: defines the mechanical closure of the P-1105 readiness gate, a gate-process rule | — | — | None | — |
| D-204 | Not implementation-bearing: replaces prose dependency phrases with unit IDs in the planning breakdown | — | — | None | — |
| D-205 | Binding product rules on the device boundary | X-017 product analytics, with F-010 and P-010 specified but unauthorized | No client event stream, analytics derived from server-held fixed-schema aggregates | Server | N-020 egress suite passes unchanged and every analytics field resolves in the observability allowlist |
| D-206 | Not implementation-bearing: defines how a landed work unit is evidenced in the planning breakdown | — | — | None | — |
| D-207 | Not implementation-bearing: absorbs duplicate work-unit identifiers and marks superseded ones, a register convention | — | — | None | — |
| D-210 | docs/privacy/ERASURE_AND_KEY_DESTRUCTION.md and ADR-022 | M-007 per-domain erasure effects and M-009 tombstones | erasure_records append-only chain and per-domain key state | Server | Erasure fixture proving key destruction, live-record deletion and an unchanged sealed generation in one serializable transaction |
| D-211 | Ranking generation contract authored by PF-052 | R-004 generation-keyed entries and R-005 atomic promotion | Sealed ranking generation with frozen content_hash and frozen aggregates | Server | Erasure-against-sealed-generation fixture showing unchanged content_hash, frozen counts and an entry that renders nothing |
| D-212 | Ranking cursor contract authored by PF-052 | R-006 immutable snapshots and viewer-bound cursors | Cursor anchored on ranking_view_id, generation and position with no subject identifier | Server/web | Stale-cursor pagination test across an erased anchor and a superseded generation with no restart or renumbering |
| D-213 | docs/privacy/ERASURE_AND_KEY_DESTRUCTION.md | M-007 per-domain erasure effects | One AES-256-GCM key per erasure domain with material present exactly when destruction time is absent | Server | key_commitment verification test plus a DDL check-constraint case rejecting both key states at once |
| D-214 | docs/operations/DATA_LIFECYCLE_AND_RECOVERY.md | M-009 restore reapplication and X-004 backup and tombstone drills | Append-only erasure journal outside the backup set and erasure_restore_receipts | Server | Restore drill replaying the journal before traffic, with a receipt row the check constraint rejects if written early |
| D-215 | packages/schemas/planning-schema.sql with the PF-048 indexing and partitioning plan | S-002 migration runner and S-011 immutable claims | Range partitions on minute_scores, notifications and audit_events, none on claims | Server | Migration apply test asserting the partitions, the concurrent index builds and the three global uniqueness invariants on claims |
| D-216 | packages/schemas/data-disposition-v1.json with docs/privacy/DATA_MAP.md as the Article 30 record | PF-050 retention registry population and M-007 retention enforcement | One retention and disposition row per persistence owner in the planning DDL | Server | Registry validator failing on a missing table, an unresolved policy key, an actorless expires_at or a journal window not one day past the backup window |
| D-217 | ADR-020 with the binding product rules | R-004 generation-keyed entries and W-003 leaderboard UX | credited_token_burn on public shapes and token_burn_total on SelfRankEntry only | Server/web | Response-shape test proving no public surface returns the raw figure or the confidence weight |
| D-218 | packages/schemas/planning-schema.sql | R-002 immutable score contributions and R-010 corrections | minute_scores, period_scores and ranking_corrections column vocabulary | Server | DDL scan proving no column named score, with registry persistence owners still resolving to tables |
| D-220 | ADR-015 and packages/schemas/openapi-v1.yaml | PF-039 session scheme decision and O-006 session and refresh rotation | Per-operation security requirements over bearerAuth, sessionCookie, refreshCookie, csrfToken and deviceProof | Server/web/native | Contract check that every operation declares its own requirement, plus an OAUTH_ISSUER_MISMATCH negative case at both completion operations |
| D-221 | ADR-015 | O-006 web and native session and refresh-family rotation | Single-use refresh handle with no idempotency record | Server/web/native | Replayed refresh handle rejected as an incident, plus the validator idempotency-exception list check |
| D-222 | packages/schemas/openapi-v1.yaml | PF-044 pagination repair and R-012 pagination concurrency suite | Opaque cursor bound to its snapshot_id and issuing principal | Server/web | Schema check that all seventeen page-returning operations declare cursor and limit, plus a foreign-cursor rejection case |
| D-223 | packages/schemas/reason-codes-v1.json | S-016 error taxonomy and reason-code mapping | Eleven operation classes derived from the OpenAPI document plus explicit operation lists | Server | validate_planning_artifacts.py failing on class-membership drift, an unclaimed 4xx status or a required code omitted |
| D-224 | packages/schemas/reason-codes-v1.json with packages/schemas/state-machine-registry-v1.json | S-016 error taxonomy and reason-code mapping | device-enrollment for two codes, fifteen still naming the unregistered vibeproof-v1 | Server | Registry cross-resolution check plus a transport-field check that the fifteen never reach an HTTP status |
| D-225 | packages/schemas/openapi-v1.yaml x-idempotency-contract | S-003 typed idempotency persistence and S-004 exact response replay | Idempotency record states reserved, committed, conflict, expired and failed | Server | Replay returning a byte-identical body under Idempotency-Replayed, with 409 conflict, 410 expiry and lease-expiry reuse cases |
| D-226 | packages/schemas/evidence-profile-policy-v1.json | PF-046 disclosure projection, partial, with W-006 evidence and disclosure UX | evidence_class on PublicProfile, RankEntry, AccountProfile and ClaimRecord | Server/web | Response-shape test that self-only fields never appear on a viewer-facing shape, plus a copy scan for provider-confirmation wording |
| D-227 | packages/schemas/openapi-v1.yaml with the approved design baseline | PF-047 schema expansion and W-003 and W-004 UX | RankEntry, PublicProfile, AccountProfile and SelfRankEntry response shapes | Server/web | Contract test that RankEntry carries no score and SelfRankEntry alone carries the raw figures, plus avatar_id resolution against packages/ui |
| D-228 | Evidence vocabulary under D-008 and ADR-020 | the storyboard module in the deleted component package (removed under D-636) | Copy strings only, no component logic or styling | Web | Banned-phrase scan over packages/ui returning no match for the four strings |
| D-230 | docs/security/ORIGIN_AND_LOOPBACK_CONTROLS.md | OS-001 origin validation and CORS and OS-014 origin and loopback machine contract | packages/schemas/origin-policy-v1.json allowlist, preflight values and check order | Server/web | go test -run Origin covering a non-allowlisted preflight and a foreign-Origin mutation, plus grep for localhost over a production build artifact |
| D-231 | docs/security/ORIGIN_AND_LOOPBACK_CONTROLS.md | OS-002 loopback listener hardening and OS-014 machine contract | Loopback session token, recorded random port and probe counter in local state | macOS, Windows, Linux | DNS-rebinding test with a foreign Host header, a bind-verification failure case and a probe rate-limit test |
| D-232 | docs/architecture/API_EDGE_CONTRACT.md with values in packages/schemas/policy-defaults-v1.json | OS-003 rate limiter | Per-operation-class and per-principal-class counters under an aggregate admission ceiling | Server | Per-class limit tests, a first-subject-rights-request-always-admitted case and a header-silence check for adaptive limits |
| D-233 | docs/architecture/API_EDGE_CONTRACT.md | OS-004 client retry, backoff and circuit breaking | Retry budget window, circuit state and daemon offline transition at 24 hours | Web/native | Jitter distribution, budget exhaustion and circuit-open tests plus a retry-safety matrix keyed on the specification rather than the HTTP method |
| D-234 | docs/architecture/API_EDGE_CONTRACT.md with ADR-013 for emergency removal | OS-011 deprecation mechanics | One path-versioned major surface with Deprecation, Sunset and Link headers | Server/web/native | Deprecation header and 180-day window test, plus an unknown-field and unknown-reason-code tolerance test in the generated clients |
| D-235 | docs/architecture/API_EDGE_CONTRACT.md and ADR-015 | OS-012 clock discipline | Server measured offset reviewed at 250 ms and refusing finalization beyond 2000 ms | Server/native | Skew fixtures at the 300-second bound, a refusal case at the 2000 ms server offset and a scan proving no interval is a wall-clock subtraction |
| D-236 | docs/operations/OBSERVABILITY_PRIVACY.md with packages/schemas/observability-allowlist-v1.yaml | OS-005 structured logging | Log field allowlist and per-environment pseudonymisation salt rotating every 90 days | Server/web/native | Compile-time allowlist check, a static-literal message lint and a test that a client-supplied request identifier is discarded |
| D-237 | docs/operations/OBSERVABILITY_PRIVACY.md | OS-006 metric instrumentation | Eighteen named metrics and four span boundaries that are logged and never exported | Server | Metric inventory test, a privacy-canary non-zero alert test and a readiness test failing outside the declared migration range |
| D-238 | ADR-018 and the operations contract | X-001 cloud-portable reference deployment, with X-013 staging contradicted by this row | Four environments local, ci, preview and production with no standing staging | Server/web | Environment inventory check, a restore drill against an ephemeral ci database and a check that production deployment is manually dispatched against a tagged release set |
| D-239 | The operations contract with cadences in packages/schemas/policy-defaults-v1.json | OS-013 secret rotation runbook and first rotation | Per-class rotation cadence with retained verification generations | Server | First rotation executed end to end, a history scan for committed secrets and a check that no cloud CI credential outlives one hour |
| D-240 | Not implementation-bearing: sets test placement and coverage policy for how work is verified, with no work unit and no product behaviour of its own | — | — | None | — |
| D-241 | docs/engineering/PERFORMANCE_BUDGETS.md | OS-010 load test harness | Six named scenarios with derived loads and pass conditions | Server | k6 scenario runs against an ephemeral ci stack before each ring expansion, with a check that results are never cited for the 100,000 scale target |
| D-242 | Conformance manifest format owned by OS-008 | OS-008 manifest format and validator and OS-009 conformance runners | Per-suite manifest of authorities, subjects and digest-pinned cases with permanent case identifiers | Server/native | Manifest validation failing a suite with no negative case, plus a runner case distinguishing a shared wrong answer from a divergence |
| D-243 | The operations contract | X-005 incident response and abuse operations and X-015 runbooks and on-call | Four commitments plus observations with review triggers and three alert classes | Server | N-020 egress suite and the deterministic duplicate and replay fixtures passing on each release, plus alert-condition tests for the six observations |
| D-244 | docs/engineering/LOCAL_DEVELOPMENT.md | OS-007 local development stack | Loopback-bound C-locale PostgreSQL container with a named volume | macOS, Linux | Planning validators running with and without a database URL, plus a check that no compose file or shared development credential is committed |
| D-245 | packages/schemas/reason-codes-v1.json, blocked on the D-224 repair | S-016 error taxonomy and reason-code mapping | conformance/adversarial/anti-cheat-registry-v1.json fault-to-code mapping | Server | Registry check that clock-rollback and future-timestamp faults no longer resolve to CLAIM_SEQUENCE_UNEXPECTED |
| D-260 | packages/schemas/accounting-arithmetic-v1.json | A-003 category containment and source authority and A-007 checked arithmetic and bounds | Unsigned 64-bit quantities carried as decimal strings in canonical component order | Native/server | A-010 order-invariance vectors plus rejection cases for overflow, containment cycles, two containers and undeclared source fields |
| D-261 | packages/schemas/accounting-arithmetic-v1.json canonical digest rule | A-001 profile loader and digest verification with P-002 deterministic CBOR | content_sha256 omitted from its own preimage across the five record kinds | Native/server | Cross-language digest vectors over the three registered profiles and rejection cases for float and boolean preimage members |
| D-262 | packages/schemas/accounting-profile.schema.json | A-005 retry, cancellation and nested-agent reconciliation | Attempt identity and the per-enum control that prevents the double count | Native | Per-value fixtures proving the deduplication control and an unknown cancellation remainder recorded as absent rather than zero |
| D-263 | Accounting contract correction rule | R-002 immutable score contributions and R-010 corrections and rebuild equivalence | Append-only directed contributions with generation supersession | Server | Rebuild equivalence fixtures plus a rejection case for retractions summing past the additions they correct |
| D-264 | packages/schemas/producer-accounting-binding-v1.schema.json | PF-041 OpenTelemetry accounting profile and V-003 compatibility tuple and capability probe runtime | Certification state driving effective_ceiling on every producer binding | Native/server | Check that every binding below active resolves to private-analytics and that no empty certification implies an exercised tuple |
| D-265 | Source receipt contract authored by PF-042 | N-008 commitment, receipt and queue stores | One device-local receipt per NormalizedAccountingEvent | macOS, Windows, Linux | N-020 egress suite proving no receipt field crosses the boundary, plus a V-008 case where the receipt explains a discarded observation |
| D-266 | packages/schemas/evidence-bundle-v1.cddl | N-001 local schema and encrypted storage with N-008 receipt stores | At-rest bundle binding claim bytes, receipt, profile, arithmetic and privacy decision by digest | macOS, Windows, Linux | Diff proving no new field in packages/schemas/egress-allowlist-v1.json plus a digest-binding test over tampered members |
| D-267 | packages/schemas/appraisal-result-v1.schema.json with packages/schemas/appraisal-policy-v1.json | PF-043 appraisal contracts and S-011 immutable claims and appraisals | Appraisal record with validity interval and supersession chain | Server | Validator comparing unbound_fields and dropped_columns against the DDL, plus a CBOR-to-JSON dimension ordinal agreement test |
| D-268 | packages/schemas/appraisal-policy-v1.json binding packages/schemas/evidence-profile-policy-v1.json by digest | PF-043 appraisal contracts and S-014 compatibility and certification policy persistence | Wire ordinals, verifier implementation digest and the E1-S and E1-R limbs | Server | Digest-binding check failing on an unmirrored edit to the bound file, plus a limb-to-base-enum mapping test |
| D-269 | packages/schemas/observer-equivalence-v1.json | A-006 duplicate-domain engine and V-008 multi-observer duplicate reconciliation | duplicate_domain_commitment preimage restricted to source-derived facts | Native/server | Two-observer fixtures that must collide, a quarantine case for disagreeing collectors and a scan rejecting observer-derived preimage inputs |
| D-280 | PF-069 private-beta invite aggregate | PF-069 specification, redemption handler unit not yet authored | invite-code machine over invite_codes and invite_redemptions | Server/web | redeemInvite 4xx set resolving against the reason registry in both directions, plus a rejected multi-use code fixture |
| D-281 | PF-069 private-beta invite aggregate | PF-069 specification, issuance tool not yet authored | Code stored only as a salted SHA-256 digest, never in plaintext | Server | Entropy and encoding vectors plus a Crockford confusable-folding test in which a mistyped variant still redeems |
| D-282 | PF-069 private-beta invite aggregate | PF-069 specification, serializable redemption transaction not yet authored | invite_redemptions primary key on invite_code_id with unique account_id | Server | Concurrent redemption test where exactly one insert commits at every isolation level, plus check-constraint cases binding invite_codes.state to its timestamp set |
| D-283 | `docs/security/PRIVATE_BETA_ADMISSION.md` with `docs/architecture/API_EDGE_CONTRACT.md` | PF-069, with the admission path in Epic O | `invite-code` states and the four admission reason codes in `reason-codes-v1.json` | Server | Ordered-gate tests asserting no reason code is emitted before authentication and that unknown, expired, revoked and spent codes return one indistinguishable 422 |
| D-284 | `docs/privacy/DATA_MAP.md` with `docs/security/PRIVATE_BETA_ADMISSION.md` | PF-069 | `invite_redemptions` as a single depth-one row per admitted account with no ancestry column | Server | Schema test proving no issuer or ancestry field is reachable from an invitee response, plus an Article 30 test that the edge carries a lawful basis and a retention window |
| D-285 | `docs/security/PRIVATE_BETA_ADMISSION.md` | PF-069 issuance tool; no unit owns an administrative HTTP route | `invite_outstanding_max` enforced by the issuing tool and deliberately not by a database constraint | Server | OpenAPI scan proving no administrative operation exists, plus a tool test refusing issuance past the outstanding-code quota |
| D-286 | `docs/security/PRIVATE_BETA_ADMISSION.md` with `docs/security/RANKED_IDENTITY_ELIGIBILITY.md` | PF-069, with the `auth-start` rate-limit class in OS-003 | Edge-side lockout counter beside the rate-limit buckets, with no persistence owner | Server | Rate-limit and lockout tests at 10 attempts per hour per address and 5 consecutive failures for 24 hours, plus a check that no path treats redemption as evidence about a person |
| D-287 | `packages/schemas/data-disposition-v1.json` with `docs/privacy/PRIVACY_CONTRACT.md` | PF-069, with the erasure effects in M-007 | `invite-code` terminal `retired` and redemption deletion in one transaction | Server | Erasure test asserting the redemption row is gone, the code reads `retired`, and a second redemption of that code fails |
| D-288 | `packages/schemas/openapi-v1.yaml` with `docs/security/PRIVATE_BETA_ADMISSION.md` | PF-069 admission middleware, surfaced through W-001 | `AccountProfile.admitted` as a boolean that discloses no invite state | Server and web | Route test asserting exactly the six named operations answer without a redemption and every other operation answers 403 `INVITE_REQUIRED` |

<!-- end generated: decision-traceability -->
