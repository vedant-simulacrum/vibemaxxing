# Decisions D-300 through D-394

<!-- generated: decision-traceability -->

| ID | Normative owner | Implementation owner/work units | Machine/state ownership | Platform scope | Required executable evidence |
|---|---|---|---|---|---|
| D-300 | Not implementation-bearing: proposes a severity regrading of planning findings that awaits owner approval and changes no registry, count or gate record | — | — | None | — |
| D-320 | Not implementation-bearing: byte-identical duplicate of D-380, which is the copy every artifact cites | — | — | None | — |
| D-321 | Not implementation-bearing: byte-identical duplicate of D-381, which is the copy every artifact cites | — | — | None | — |
| D-322 | Not implementation-bearing: byte-identical duplicate of D-382, which is the copy every artifact cites | — | — | None | — |
| D-323 | Not implementation-bearing: byte-identical duplicate of D-383, which is the copy every artifact cites | — | — | None | — |
| D-324 | Not implementation-bearing: byte-identical duplicate of D-384, which is the copy every artifact cites | — | — | None | — |
| D-325 | Not implementation-bearing: byte-identical duplicate of D-385, which is the copy every artifact cites | — | — | None | — |
| D-326 | Not implementation-bearing: byte-identical duplicate of D-386, which is the copy every artifact cites | — | — | None | — |
| D-327 | Not implementation-bearing: byte-identical duplicate of D-387, which is the copy every artifact cites | — | — | None | — |
| D-328 | Not implementation-bearing: byte-identical duplicate of D-388, which is the copy every artifact cites | — | — | None | — |
| D-329 | Not implementation-bearing: byte-identical duplicate of D-389, which is the copy every artifact cites | — | — | None | — |
| D-330 | Not implementation-bearing: byte-identical duplicate of D-390, which is the copy every artifact cites | — | — | None | — |
| D-331 | Not implementation-bearing: byte-identical duplicate of D-391, which is the copy every artifact cites | — | — | None | — |
| D-332 | Not implementation-bearing: byte-identical duplicate of D-392, which is the copy every artifact cites | — | — | None | — |
| D-333 | Not implementation-bearing: byte-identical duplicate of D-393, which is the copy every artifact cites | — | — | None | — |
| D-334 | Not implementation-bearing: byte-identical duplicate of D-394, which is the copy every artifact cites | — | — | None | — |
| D-340 | `docs/architecture/VIBEPROOF_V1_PROTOCOL.md` ZIP-215 pin | PF-068, consumed by P-003 | None; a conformance fixture with no aggregate or machine | Native and server | `python3 -m unittest tests.ci.test_ed25519_divergence_corpus` asserting each of non-canonical `A`, non-canonical `R`, small-order `A` and `S >= l` is separated by at least one case |
| D-341 | `conformance/vibeproof/v1/README.md` corpus contract | PF-068 generator `scripts/repository/generate_ed25519_divergence_corpus.py` | Per-case `confirmed` or `unconfirmed` verdict bound to a SHA-256 over the case bytes | None | `generate_ed25519_divergence_corpus.py --check` refusing to write when a recorded oracle result contradicts the computed verdict or the bound digest moves |
| D-342 | `conformance/vibeproof/v1/README.md` with the D-012 crate bakeoff | PF-068 oracle module `conformance/vibeproof/v1/zip215-oracle/`; product verifier selection stays in Epic P | None; oracle module isolated from every product path | None | Import-graph check proving no product path reaches the oracle module, plus a test failing when a contrast implementation is recorded as the ZIP-215 verdict |
| D-343 | `conformance/vibeproof/v1/README.md` corpus contract | PF-068 | `against_strict_rfc8032` and `against_observed_cofactorless` as two separate per-case facts | None | `run_ed25519_oracles.py` refresh that reruns both contrast oracles and fails when an observed verdict moves from the recorded run |
| D-344 | `docs/architecture/VIBEPROOF_V1_PROTOCOL.md` | PF-068 corpus, consumed by P-003 alongside the exact-byte vectors | None; a test-only signer seed shared with `conformance/vibeproof/v1/exact-byte-vectors.json` | Native and server | P-003 acceptance run over both the divergence corpus and the exact-byte vectors, failing when either file is unconsumed |
| D-345 | Not implementation-bearing: corrects a work-unit dependency edge in the breakdown, which is scheduling metadata rather than product behaviour | — | — | None | — |
| D-360 | `docs/decisions/ADR-017-HOSTING_REGION_AND_RESIDENCY.md` | X-016, measured by X-009 and X-014 | None; operator cost figures with no product aggregate | Server | `python3 scripts/ci/check_cost_model.py` requiring a measured driver per component and failing when a credit balance stands in for a steady-state figure |
| D-361 | `docs/decisions/ADR-017-HOSTING_REGION_AND_RESIDENCY.md` | X-001 | None; deployment configuration rather than a mutable aggregate | Server | `terraform validate` and `terraform plan` against the reference configuration in an EU region, plus the X-004 restore-elsewhere drill |
| D-362 | `docs/decisions/ADR-017-HOSTING_REGION_AND_RESIDENCY.md` | X-001 | None; managed-service exclusion list checked against the plan | Server | Plan check failing on any managed service outside managed PostgreSQL, containers and object storage on the critical path |
| D-363 | `docs/decisions/ADR-017-HOSTING_REGION_AND_RESIDENCY.md` | X-016 | None; a funding runway rather than a persisted state | Server | Cost-model check that fails when a budget gate is satisfied by credits rather than by a measured steady-state cost |
| D-380 | `docs/security/AUTHENTICATION_AND_RECOVERY.md` with `packages/schemas/recovery-case-v1.schema.json` | PF-007, executed by O-008 and O-013 | `recovery-case` machine over `recovery_cases`, cooling-off check constraint and live-case partial unique index | Server | Constraint tests proving an apply before `cooling_off_ends_at` and a second live case are both unwritable, and that an applied case revoked every session family |
| D-381 | `docs/security/RANKED_IDENTITY_ELIGIBILITY.md` | PF-008, executed by O-009 | `identity-investigation` machine over `identity_investigations`, integrity-private with no `reversed` state | Server | Projection tests proving no surface discloses an open case, plus a state test that a conclusion reverses nothing without an `appeals` outcome |
| D-382 | `packages/schemas/consolidation-plan-v1.schema.json` with `docs/security/RANKED_IDENTITY_ELIGIBILITY.md` | PF-008, executed by O-010 | `consolidation_cases` and `consolidation_contributions`, `applied` non-terminal and `reversed` appending inverse contributions | Server | `validate_planning_artifacts.py` refusing a combined-total field, plus an overlapping-claims fixture counting a duplicate domain commitment once |
| D-383 | `docs/security/INTEGRITY_MODEL.md` | PF-010, persisted by S-008 | `lineage_fork_cases` and `lineage_fork_branches`, `unresolved` non-terminal, quarantined claims held | Server | Constraint tests that a resumed generation is strictly greater than the fork generation and that two survivors in one case are unwritable, over conformance/vibeproof/v1/fork-and-rotation-vectors.json |
| D-384 | `docs/architecture/NATIVE_RUNTIME_AND_STORAGE_CONTRACT.md` with `packages/schemas/local-store-v1.sql` | PF-014, implemented by N-001 and N-002 | SQLite device store, commitment written before a claim is offered, per-device deletion receipt | macOS, Windows, Linux | `python3 -m unittest tests.ci.test_local_store_contract` plus the N-001 raw-file canary scan over `p1140b-boundary-canaries-v1.json` |
| D-385 | `packages/schemas/presence-pulse-v1.schema.json` with `packages/schemas/policy-defaults-v1.json` | PF-026, implemented by G-009 and G-010 | `presence` machine thresholds and `presence_events` at `no-retention` | Server | Validator run asserting the three resolved values 30, 90 and 300 against their `const` bindings, failing when a value moves without its meaning |
| D-386 | `packages/schemas/projection-authorization-v1.json` with `docs/privacy/PRIVACY_CONTRACT.md` | PF-033, applied by G-011 and G-015 | Nine named inputs, `blocks.established_at`, no cached authorization result | Server | Race test proving a revision recheck at emit time fails closed, and a notification test proving authorization is rechecked at delivery rather than creation |
| D-387 | `packages/schemas/compatibility-tuple-v1.schema.json` with `docs/integrations/UNIVERSAL_AGENT_COMPATIBILITY.md` | PF-015, exercised by V-003 | Tuple digest over six dimensions, observation mode drawn from `observer-equivalence-v1.json` | Native and server | `python3 -m unittest tests.ci.test_compatibility_tuple_digest`, extended with a case refusing an open-ended upper version bound |
| D-388 | `docs/integrations/ADAPTER_CERTIFICATION_POLICY.md` with `packages/schemas/certification-result-v1.schema.json` | PF-016, exercised by V-006 and V-010 | `source-certification` machine over `source_certifications` with append-only `certification_results` | Server | Check-constraint tests that any state other than `active` cannot sit beside a ceiling above `private-analytics` and that a passing result with zero negative cases is unwritable |
| D-389 | `packages/schemas/install-plan-v1.schema.json` with `docs/architecture/PLATFORM_KEY_AND_PRIVILEGE_MATRIX.md` | PF-032, implemented by L-008, L-009 and L-010 | `platform_install_plans` and `platform_install_operations`, each operation naming its reversal or declaring none | macOS, Windows, Linux | Schema tests pinning `verify-release-signature` at sequence 1 and requiring `requires_privileged_consent` on any plan containing `register-privileged-supervisor` |
| D-390 | `packages/schemas/tuf-trust-v1.schema.json` with `docs/operations/OPERATIONS_OPEN_SOURCE_AND_LAUNCH_CONTRACT.md` | PF-030, implemented by L-001 | `tuf_metadata` keyed on device and role, signature count against threshold as a check constraint | Native and server | Hostile-metadata fixtures per role asserting the rollback, freeze and below-threshold refusals, and that expiry is enforced rather than warned about |
| D-391 | `packages/schemas/compatibility-graph-v1.schema.json` | PF-031, implemented by L-004 | `compatibility_edges`, one row per relation across six independently versioned interfaces | Native and server | Schema tests refusing an empty range, a derived `breaking` flag, and a sunset with no deprecation notice outside the three D-234 carve-outs |
| D-392 | `packages/schemas/migration-chain-v1.schema.json` | PF-031, implemented by L-004 and L-007 | `storage_migrations` rollback class beside each `schema_migrations` version, `down_sql_present` held separately | Native and server | Constraint test requiring a snapshot digest for `snapshot-required`, plus a case proving a present down section does not yield a reversible class |
| D-393 | `packages/schemas/disclosure-projection-v1.json` | PF-046, consumed by W-001 | Per-shape audience classification with `SelfRankEntry` as the narrow shape and no per-response redaction | Server and web | `validate_planning_artifacts.py` resolving every field against `openapi-v1.yaml` and failing on an unclassified or narrower field, plus a test pinning `token_burn_total` and the three ADR-020 weights to `self` |
| D-394 | `packages/schemas/ui-state-projection-v1.json` | W-010, bound to PF-033 | Each exceptional state resolved to a registered machine, a viewer-authorization input, or declared client-local | Web | Matrix test asserting `blocked` and `private` render indistinguishably from an absent subject and that no filtered count is emitted for `empty` |

<!-- end generated: decision-traceability -->
