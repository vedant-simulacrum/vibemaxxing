# VibeMaxxing Decision Register

Updated: 2026-08-06

Allowed statuses: `accepted`, `provisional`, `research-required`, `deferred`, `rejected`, `superseded`.

| ID | Decision | Status | Validation or reopen condition |
|---|---|---|---|
| D-001 | Greenfield VibeMaxxing; no migration of old accounts or scores | accepted | explicit product reset |
| D-002 | Product thesis: Codex restraint × Steam social competition | accepted | explicit strategy change |
| D-003 | Visual thesis: The Competitive Ledger | accepted | tested design failure |
| D-004 | Token Burn is the default raw-volume ranking metric | accepted | evidence that it materially misleads competition |
| D-005 | Cash Burn is always labelled estimated, never actual spend | accepted | explicit product approval |
| D-006 | Forbidden content never crosses the device boundary | accepted | never silently reopen |
| D-007 | Historical imports are private analytics only and never affect active competition | accepted | explicit competition-policy change |
| D-008 | Public evidence labels are Standard, Hardened, and Imported | accepted | validated comprehension failure |
| D-009 | Development is local-first; no remote coding control plane | accepted | explicit user reversal |
| D-010 | Rust native/protocol core, Go server, Next.js/TypeScript web, PostgreSQL/pgx | accepted | benchmarked ADR showing material benefit |
| D-011 | Deterministic CBOR + CDDL + COSE_Sign1 for signed public claims | accepted | conformance failure requiring a protocol ADR |
| D-012 | Final Rust CBOR/COSE implementation crates are selected during implementation bakeoff behind an internal boundary | research-required | malformed, fuzz, differential and resource tests pass |
| D-013 | OS-specific isolation; never claim equal sandbox strength | accepted | platform evidence changes |
| D-014 | Local IPC combines OS peer identity, ACLs, challenge-response, versioning and limits | accepted | adversarial validation disproves design |
| D-015 | Device identities are revocable Ed25519 public keys with sequence/hash continuity and explicit rotation | accepted | cryptographic or platform evidence requires revision |
| D-016 | Passkeys are mandatory primary authentication | superseded | replaced by D-028 |
| D-017 | Append-only claim ledger + transactional outbox + idempotent aggregates | accepted | benchmark or correctness failure |
| D-018 | Competition ranking uses SQL `rank()` gaps, with stable display tie-breakers | accepted | user testing or simulation demonstrates material harm |
| D-019 | Pricing uses immutable versioned datasets with provenance and effective dates | accepted | evidence-backed ADR |
| D-020 | Country boards are part of launch scope | superseded | replaced by D-052 |
| D-021 | Integrity enforcement is progressive, appealable and does not require identity documents by default | accepted | severe integrity evidence |
| D-022 | Public support claims are generated from an exercised adapter registry | accepted | none |
| D-023 | Three named CLI adapters are the complete launch scope | superseded | replaced by D-030 |
| D-024 | Secure updater uses TUF and must pass conformance and hostile-metadata tests | accepted | updater ADR |
| D-025 | Telemetry is allowlisted and content-bearing fields are forbidden | accepted | never silently reopen |
| D-026 | Kubernetes, Kafka, GraphQL, service mesh, vector DB, workflow engine and ORM-heavy persistence are excluded absent ADR evidence | accepted | evidence-backed ADR |
| D-027 | Product implementation requires explicit user phase approval | accepted | explicit user phase change |
| D-028 | Primary account access is OAuth-based; passkeys/hardware credentials are optional stronger factors | accepted | provider or security research failure |
| D-029 | Internal delivery is staged, but public launch targets the complete initial product | accepted | explicit user scope change |
| D-030 | Agent coverage is a universal tiered compatibility system with certified, generic, imported and unsupported states | accepted | ecosystem or feasibility evidence |
| D-031 | Local UX includes daemon, private collector, network-safe sync, CLI, macOS menu bar, Windows/Linux tray, local controls and hosted web | accepted | platform feasibility evidence |
| D-032 | Genuine but intentionally wasteful usage counts when authentic and non-duplicated | accepted | explicit product-policy change |
| D-033 | Repository is private during planning and becomes public open source before public launch | accepted | explicit user change |
| D-034 | Product CI, eval, dependency, security and release automation remains manual-only during planning; read-only planning validation is allowed | accepted | implementation phase begins |
| D-035 | An SLM is conditional residual-risk detection and may not rewrite totals or permanently ban users | superseded | replaced by D-053 |
| D-036 | GitHub uses a GitHub App with web/device authorization; X uses OAuth 2.0 Authorization Code with PKCE | accepted | provider capability or security changes |
| D-037 | Raw Token Burn remains unnormalized across model capability; filters and disclosures provide context | accepted | explicit new metric ADR |
| D-038 | Technical planning contracts were previously declared complete | superseded | replaced by D-042 after audit found missing schemas and contradictions |
| D-039 | Initial production architecture is cloud-portable managed containers, PostgreSQL, optional Redis and object storage | accepted | provider ADR during implementation |
| D-040 | Apache-2.0 for original code, CC BY 4.0 for docs/specs, DCO without CLA initially | accepted | ADR-009; final dependency/counsel review remains release evidence |
| D-041 | `AGENTS.md` is the sole initialization manual; `docs/project/` owns project authority/status/navigation; implementation planning is limited to one handoff plus one PR work breakdown; research is indexed only through `docs/research/README.md` | accepted | explicit repository-governance change |
| D-042 | Repository remains in planning-hardening until draft schemas, validation, governance and protocol edge semantics pass | superseded | replaced by D-045 after the planning-hardening gates passed |
| D-043 | Protocol v1 uses one challenge per atomic batch; partial acceptance is prohibited; bounded signed gap declarations downgrade continuity | provisional | reopened by P-1140C because the current batch, checkpoint and sequence contracts conflict |
| D-044 | Handle normalization uses Unicode 16.0 NFKC/full case fold/confusable skeleton rules; configurable defaults live in a versioned policy registry | accepted | ADR-008 or proven internationalization failure |
| D-045 | Technical planning is complete at validated contract level; implementation remains inactive until explicit P-1104 approval | superseded | replaced by D-049 after the July 23 audit found cross-contract P0/P1 contradictions |
| D-046 | T20 is the rolling golden-path engineering cohort; each active slot requires exact multidimensional certification plus quantitative optimization evidence, while non-T20 models may use lower honest support tiers | provisional | reconcile T20 launch value with capability-based adapter rollout and real usage evidence under P-1140B/E |
| D-047 | Repository artifacts are classified as specification, mock, runnable prototype, production implementation, or executable evidence; the existing fixture-backed web app is a bounded runnable prototype and does not authorize further implementation | accepted | explicit phase change or artifact evidence changes |
| D-048 | Light-mode leaderboard bento is the approved first-screen visual baseline; dark mode and the Signal Ledger, Trackside, and Duel directions are rejected | accepted | explicit visual-direction change after rendered frontend review |
| D-049 | Repository phase is planning alignment and contract repair under P-1140A–E; P-1104 remains blocked afterward until explicit approval | accepted | P-1140A–E complete, clean validation passes and user explicitly opens implementation |
| D-050 | Client claims contain evidence facts only; a server verifier creates the authoritative appraisal and public Standard/Hardened state | accepted | protocol/security evidence requires revision |
| D-051 | Deterministically captured local-model and delayed offline usage may compete; Standard and Hardened both count globally, while Imported never counts | accepted | explicit competition-policy change or material integrity evidence |
| D-052 | Country leaderboards are postponed until post-launch and are removed from launch requirements, routes and readiness gates | accepted | explicit user reversal after country semantics, privacy and moderation contracts mature |
| D-053 | The SLM detector is post-launch research only: local, sandboxed, advisory, non-authoritative and promoted only after measured lift over simpler baselines | accepted | reproducible bakeoff and explicit launch-scope change |
| D-054 | Launch identity strongly enforces one active ranked identity per detected/resolved person without claiming mathematically verified unique humans or requiring government ID/biometrics by default | accepted | severe integrity failure or explicit privacy/identity change |
| D-055 | GitHub and X are the launch authentication providers; Google is deferred until auth, API, persistence, recovery and policy contracts add it coherently | accepted | provider availability or explicit scope change |
| D-056 | VibeProof v1 may be rewritten incompatibly before implementation; no draft field has compatibility protection | accepted | production protocol ships |
| D-057 | Kernel anti-cheat and mandatory VibeMaxxing inference proxying are rejected for the default product | accepted | explicit product/privacy reversal supported by evidence |
| D-058 | Adapter, collector, detector and release trust is digest-addressed and provenance-bound; mutable names or versions alone never establish official identity | accepted | supply-chain evidence requires revision |
| D-059 | Public launch targets the complete core social product except countries; internal staging does not permit weak or placeholder launch behavior | accepted | explicit user scope change |
| D-060 | Open PR #17 is superseded by the July 23 audit, launch decisions, privacy and anti-cheat work and must not be merged unchanged | accepted | none |
| D-061 | `vibemaxxing-daemon` is an always-on OS-supervised per-user background service: enabled at installation, auto-started, automatically restarted, independent of shell state, and resident through paused/offline/degraded/recovery states; platform-imposed session boundaries must be disclosed honestly | accepted | explicit user reversal or executable evidence that the selected platform mechanism cannot meet ADR-010 |
| D-062 | macOS launch support includes both Apple silicon `arm64` and Intel `x86_64` under rolling exact-profile certification | accepted | explicit scope reversal or inability to satisfy release/security gates |
| D-063 | Windows launch support includes native `x64` and native `ARM64` across maintained desktop and applicable Server profiles | accepted | explicit scope reversal or upstream/toolchain infeasibility proven by evidence |
| D-064 | Linux launch support is broad and rolling across maintained desktop/headless distributions, major package ecosystems, `x86_64` and `aarch64`, with exact profile certification | accepted | explicit scope reversal or evidence-backed profile reduction |
| D-065 | WSL, containers and CI/ephemeral runners are globally competitive by default at the verifier-awarded evidence level; boards may impose stronger minimums | accepted | severe integrity failure or explicit competition-policy change |
| D-066 | Android, iOS, iPadOS and ChromeOS have no native collector, companion, control or launch application scope; hosted web remains an ordinary browser surface | accepted | explicit user reversal through a new ADR |
| D-067 | Optional machine-wide privileged supervision is allowed as a separately consented, least-privilege lifecycle profile that cannot inspect source content or bypass user isolation | accepted | independent review failure or explicit privilege-policy reversal |
| D-068 | Automatic updates are mandatory for competitive profiles, with signed release-set verification, bounded deferral, rollback and environment-specific mechanisms | accepted | explicit product reversal or updater evidence requires revision |
| D-069 | Automated Storybook capture is permitted only as read-only prototype/design validation and cannot satisfy product implementation, security or launch gates | accepted | implementation phase opens or workflow scope materially expands |
| D-070 | Duplicate-account consolidation combines valid historical competitive claim contributions under one surviving ranked identity, preserving original period attribution and corrections; stored account totals are never added together, imported records remain excluded, and overlapping or duplicate-domain contributions count once | accepted | explicit user reversal or demonstrated inability to reconstruct non-overlapping contributions safely |
| D-071 | Board invitations may grant only non-privileged membership roles; administrator promotion and ownership transfer are separate recent-authenticated, revision-checked, audited operations | accepted | explicit user reversal after abuse and recovery analysis |
| D-072 | A detected lineage fork quarantines all post-fork branches, preserves accepted pre-fork claims, selects or recovers one survivor where possible, resumes through a new lineage generation, never merges commitment chains, and remains appealable | accepted | cryptographic or recovery evidence requires a safer fork-resolution protocol |
| D-073 | Presence uses qualifying native pulses every 30 seconds, becomes idle after 90 seconds without a qualifying pulse, and becomes offline after 300 seconds; private is an independent viewer-visibility policy | accepted | measured battery/network evidence or user comprehension testing requires adjustment |
| D-074 | Automatic binary rollback is allowed only while the previous release remains read/write compatible with every committed local-state and database mutation; after an irreversible migration recovery is roll-forward or restoration of a verified pre-migration snapshot | accepted | implementation evidence proves a safer equivalent recovery mechanism |
| D-075 | High-impact idempotency records retain the exact replayable result for at least 30 days; claim-batch results remain replayable until a later acknowledged checkpoint safely supersedes them; expired keys are rejected rather than silently treated as new mutations | accepted | measured retention cost or recovery evidence supports a safer operation-specific window |
| D-076 | Hosted deletion and each local-device deletion are reported independently; the product never claims all local data erased while any device is offline, expired, unreachable, waived, or unverified | accepted | explicit user reversal or stronger independently verifiable local-erasure evidence |
| D-077 | Only provider-signed receipts or server-side retrieval under verified account binding may be labelled source-bound; device-signed, adapter-certified, or locally observed evidence is attested-local and cannot be elevated to source-bound by an SLM or client assertion | accepted | a new provider or platform attestation standard proves equivalent external-event authority |
| D-078 | E1 splits into E1-S provider-signed receipts, which remain unavailable, and E1-R provider-retrieved organization aggregates, which organization, hacker-house and community boards may enrol under ADR-016 as a board-scope corroboration input that never binds an individual claim, alters raw score, or reaches Hardened; individual and global boards remain self-reported because no provider exposes an individual usage authorization scope | accepted | a provider ships an individual usage authorization scope or a signed per-claim receipt, or withdraws administrative usage retrieval |
| D-079 | State identifiers use kebab-case in every source — the state-machine registry, SQL CHECK vocabularies, and OpenAPI enums — so one literal spelling appears in all three and no boundary needs a transform. Chosen over snake_case because `state-machine-registry-v1.schema.json` constrains identifiers to `^[a-z0-9]+(?:[.-][a-z0-9]+)*$`, which cannot express an underscore; adopting snake_case would have required either changing that schema or reintroducing the per-boundary mapping that PF-038 exists to remove. Enforced by `scripts/repository/validate_state_vocabularies.py`. | accepted | the registry schema's identifier pattern changes, or a downstream consumer cannot express kebab-case |
| D-080 | Hosted infrastructure is primarily located in the European Union and the product is built GDPR-native from day one rather than retrofitted; the hosting provider is deliberately not selected by this decision and is decided by ADR-017 against written criteria | accepted | a residency, latency or cost obligation that EU-primary hosting cannot satisfy, evidenced against ADR-017's criteria |
| D-081 | A linked provider account must be at least 90 days old, measured from the provider-reported account creation timestamp, before it can support a ranked identity; the OAuth flow captures and persists that timestamp, which is a new personal-data field subject to the privacy contract, the data map and the erasure path. This adds an eligibility precondition to the launch providers already fixed by D-055 and does not change which providers are supported | accepted | measured evidence that the gate blocks materially more legitimate new participants than Sybil accounts, or a provider stops exposing an account creation timestamp |
| D-082 | Public rank is computed on a credited Token Burn figure, not on the raw reported figure. Credited burn is raw burn multiplied by a server-assigned confidence weight that is a function of the awarded evidence profile and the ranked-identity trust state. Raw Token Burn remains the stored, immutable, unnormalized accounting quantity under D-004 and D-037; the weight is applied at ranking projection and never rewrites an accepted claim. ADR-020 owns the function, its bounds and its disclosure | accepted | measured evidence that weighting materially misranks honest participants, or a self-evidencing external usage source removes the need for it |
| D-083 | The awarded evidence profile is a ranking weight, not decoration. Every profile that is competitively eligible appears on the same default board and its profile determines the confidence weight applied to that entry. No second eligibility-filtered board is introduced, and no board is restricted to one evidence profile at launch | accepted | comprehension testing shows users read a weighted board as a filtered one, or a board audience requires a hard eligibility floor rather than a weight |
| D-084 | An integrity sanction is silent toward the public and notified toward the sanctioned participant. The public leaderboard shows no sanction marker, the affected participant receives a server-inbox notice stating the effect and the appeal route, and a first sanction is reversible with the ranking effect fully reversed on a successful appeal. This keeps the appeal lifecycle machine and the retracted exceptional state live rather than unreachable | accepted | measured evidence that private notice materially accelerates evasion tooling, or an abuse pattern requires a public integrity signal |
| D-085 | A GDPR Article 17 erasure request removes the account, the ranked identity and that identity's historical ranking entries, so that no erased participant remains visible or reconstructible in any published standing. The outcome is unchanged from the provisional form; what was missing was the mechanism, and D-210 supplies it: a signed erasure record is appended and the key binding the retained pseudonym to the person is destroyed. Nothing is deleted from a sealed generation, so the immutability rule and the durable cursors both survive intact | accepted | a supervisory authority, a court or engaged counsel holds that key destruction is not erasure for data of this kind, which would force deletion of the sealed generations themselves and reopen the immutability rule rather than this row; or a demonstrated re-identification of a key-destroyed entry by any route |
| D-086 | Launch notification delivery is the server inbox only. No push channel and no email channel ships at launch, consistent with the binding rule that the server inbox is notification authority and other transports are best-effort hints | accepted | the stated day-7 retention mechanism is overtake notification, which an inbox-only product delivers only once the participant opens the product; measured retention evidence that this is insufficient reopens the decision |
| D-087 | Imported historical usage renders as a single private lifetime statistic on the owner's own surface, is never ranked, never enters any board or period standing, and is labelled as imported wherever it appears. This is the first statement of what an import actually renders as; it does not relax D-007 or D-051 | accepted | explicit competition-policy change permitting imported records to affect standings |
| D-088 | Launch board scope is the full cross product of the six frozen leaderboard scopes — global, friends, private-board, organization, hacker-house and community — and the six frozen periods — daily, weekly, monthly, seasonal, yearly and lifetime. Individual boards are the headline surface and organization, hacker-house and community boards are secondary but in scope | accepted | explicit user scope change; country boards remain post-launch under D-052 |
| D-089 | Claude Code is adapter number one and is the first agent integration certified end to end | accepted | the first adapter cannot reach an exact certified source and accounting tuple, or explicit user reprioritization |
| D-090 | Deterministic CBOR, CDDL and COSE_Sign1 with Ed25519 are launch-blocking rather than deferrable, so the D-012 Rust CBOR/COSE crate bakeoff moves from implementation-time work onto the launch critical path | accepted | explicit user reversal accepting a simpler signed envelope for v1 |
| D-091 | The project runs with a single maintainer for the foreseeable future. Branch protection on `main` requires the planning checks, linear history and no force-push, and permits the owner's administrator bypass, because a second reviewer does not exist | accepted | a second maintainer joins, at which point administrator bypass is withdrawn and the dual-control obligations for release and root keys become satisfiable |
| D-092 | Service availability is best effort. There is no paging, no on-call rotation and no committed incident response time; hours of downtime are acceptable. The service-level objectives recorded in `docs/operations/SLOS_AND_ALERTS.md` and `docs/operations/OPERATIONS_OPEN_SOURCE_AND_LAUNCH_CONTRACT.md` are therefore aspirational targets and must be restated as such rather than presented as commitments | accepted | a paid tier, a second maintainer or an external obligation creates a real availability commitment |
| D-093 | Total hosted infrastructure spend is capped under 100 USD per month, satisfied from provider credits and free tiers where possible | superseded | superseded by D-360, which raises the ceiling to the measured steady-state cost of the selected configuration rather than a fixed figure |
| D-094 | The 100 USD per month ceiling of D-093, the stated 100,000 ranked-identity scale target and the PostgreSQL RPO of at most 5 minutes and RTO of at most 60 minutes recorded in `docs/operations/OPERATIONS_OPEN_SOURCE_AND_LAUNCH_CONTRACT.md` cannot all hold simultaneously; managed point-in-time recovery at that objective, 36 board projections and that identity count exceed the ceiling. This is recorded as an open conflict, not resolved, and no artifact may present all three as jointly satisfied | superseded | resolved by D-360: the owner amended the ceiling, which is one of the three permitted amendments ADR-017 step 4 names |
| D-095 | Two privacy and security risks are accepted rather than mitigated for launch, at owner direction, and are revisited post-launch: presence disclosure that makes a participant's working hours inferable to authorized viewers, and the low-bandwidth inference channel that fixed-schema aggregate token counts leave open. ADR-019 is their register and states the compensating controls, revisit signals and post-launch owner | accepted | a reported harm, a demonstrated extraction, or the post-launch review recorded in ADR-019 |
| D-096 | The Rust and Go protocol implementations are rewritten against the normative `packages/schemas/vibeproof-claim-v1.cddl` profile and the exact-byte vectors under `conformance/vibeproof/v1/`, and the reduced eleven-field shadow profile in `protocol/vibeproof-v1.cddl` together with its shadow vector corpus is retired once both implementations and their conformance suites consume the normative authority | accepted | the normative profile is proven unimplementable in one of the two languages, which would require a protocol ADR rather than a return to the shadow profile |
| D-097 | The server database is managed PostgreSQL 16 or later, and `goose` is the migration tool: plain `.sql` files with explicit up and down sections in `/migrations`, sequential numbering, applied as a deliberate operator or pipeline step and never at API startup. Chosen over `golang-migrate`, Atlas and `sqlx` in ADR-018, principally because it holds no schema model of its own and therefore cannot compete with `packages/schemas/planning-schema.sql` for authority, and because its no-transaction directive is what makes the contractually required online index creation executable | accepted | the owner declines or amends ratification, which the owner reserved when delegating this choice; or `goose` loses the no-transaction capability, changes licence, or is archived |
| D-098 | Claude Code's OpenTelemetry metrics channel is adapter one's primary capture mechanism and its session JSONL is a degraded fallback capped at private analytics. OTLP metrics are emitted after the API response with finalised counts, on a consumer subscription, into a listener the user runs, with no server-side cooperation; the JSONL substrate is unsigned, mutable and measurably wrong in both directions — roughly 174x input undercount, 91x subagent overcount, and an 11% retroactive decrease in 16 hours. When both observe one session the OTLP observation supersedes the fallback and the two are never summed. `docs/integrations/ADAPTER_ONE_CLAUDE_CODE_OTEL.md` owns the mechanism | accepted | the vendor removes or paywalls the metrics channel, or the session files gain a signature and a finalised-count guarantee that closes all three defects |
| D-099 | `user.email`, `user.id`, `user.account_id`, `user.account_uuid` and `organization.id` are stripped from every OTLP datapoint inside the receiver, before the datapoint is admitted to the observation queue and therefore before any aggregation, and are never written to disk, a log, a crash report or a diagnostic capture and never used to key any local structure. A datapoint from which they cannot be removed is rejected whole; an unrecognised attribute is dropped and flagged rather than passed. The strip does not depend on `OTEL_METRICS_INCLUDE_ACCOUNT_UUID`, because that setting removes only the account UUIDs and no documented setting removes `user.email`. Enforced by `validate_adapter_one_boundary` in `scripts/repository/validate_planning_artifacts.py` against the fixtures in `conformance/adapters/claude-code-otel/` | accepted | never silently reopen; a vendor change that removes the attributes at source reduces the exposure but does not retire the strip |
| D-100 | No provider offers a usage-attestation path for an individual account, so no VibeMaxxing claim from an individual participant is self-evidencing. Anthropic's Admin API and Claude Code Analytics API are documented as unavailable to individual accounts and require an organization-provisioned administrator key; OpenAI's usage endpoints require an Admin key held by an Organization Owner; every Cursor endpoint is team-scoped. This is recorded as a structural product constraint rather than an integration gap: it is why D-078 leaves individual and global boards self-reported, why ADR-016's organization-scoped corroboration never binds an individual claim, and why the ADR-020 confidence weight rather than source attestation carries the integrity load | accepted | a provider ships an individual-account usage authorization scope or a signed per-claim receipt |
| D-101 | Consent under Article 6(1)(a) of Regulation (EU) 2016/679 is the lawful basis for every participant-facing purpose: account creation, provider linking, usage collection, publication of handle and score, social relationships, presence and notifications. Article 6(1)(b) is rejected because EDPB Guidelines 2/2019 hold that engagement metrics are not objectively necessary to perform a contract, and a leaderboard is an engagement metric. Article 6(1)(f) is used only for network and information security and fraud prevention under Recital 49, and Article 6(1)(c) only for records the law compels. Consent is genuinely refusable under Article 7(4) and Recital 43, and a basis is never swapped after withdrawal | accepted | a supervisory authority, a court or engaged counsel holds that a different basis is required or available, or the product acquires a function that a participant can use without being published |
| D-102 | The global leaderboard remains public by default, European Union participants included, as an informed risk acceptance recorded in ADR-021. Aggregate token counts tied to a handle are personal data under Article 4(1) and Recital 30; they are not Article 9 special-category data; the processing meets five of the nine WP248 rev.01 criteria where two suffice; and the position under Article 25(2) is defensible rather than certain. The owner decided with that analysis in front of them | accepted | a complaint, a supervisory-authority letter, a reading of Article 25(2) that requires private-by-default for a public ranking of named individuals, or engagement of counsel |
| D-103 | The minimum age is 16 everywhere, as a single European Union floor set at the ceiling Article 8(1) permits rather than at each member state's own digital-consent age, which ranges from 13 to 16. A uniform floor removes the need to determine a participant's member state and the need for verifiable parental consent, both of which would require collecting more personal data about minors than the floor does | accepted | a member state obligation that a uniform floor cannot satisfy, or evidence that the floor excludes a material legitimate population |
| D-104 | Article 5(3) of Directive 2002/58/EC is a second, independent consent requirement that the local daemon must satisfy before reading information stored on a participant's terminal equipment, separate from and not satisfied by the GDPR basis. EDPB Guidelines 2/2023 make the provision technology-neutral and not confined to cookies. The strictly-necessary exemption is not relied on, because the access exists to produce a ranking rather than to carry out a transmission. Consent is per agent source, separately refusable and separately withdrawable, and refusing one source does not disable the rest of the product | accepted | an authoritative reading that the strictly-necessary exemption covers local usage collection, or a national implementation that requires a different mechanism |
| D-105 | An Article 30 record of processing activities is mandatory and `docs/privacy/DATA_MAP.md` is that record. The Article 30(5) under-250 exemption does not apply: the Article 29 Working Party position paper of 19 April 2018 confirms the three carve-outs are alternative rather than cumulative, continuous collection from an always-on daemon is not occasional, and D-102 records the risk assessment | accepted | the processing becomes genuinely occasional and low risk, which would require removing continuous collection |
| D-106 | No Article 27 representative in the Union is required or appointed, because the controller is established in the Union and Article 3(1) applies directly, which makes Article 27 definitionally inapplicable. A United Kingdom representative under Article 27 of the UK GDPR is required before participants in the United Kingdom are accepted, and none is appointed; accepting UK participants is gated on appointing one | accepted | the controller ceases to be established in the Union, or United Kingdom participants are accepted |
| D-107 | No United States state comprehensive privacy law applies, because each is gated on thresholds the controller does not meet — the California Consumer Privacy Act requires a for-profit business with either 26.625 million dollars of revenue or 100,000 consumers. Two obligations apply with no threshold and are accepted: CalOPPA at California Business and Professions Code section 22575(a), satisfied by publishing `PRIVACY.md`, and breach notification in all fifty states, where California Civil Code section 1798.82(a) binds an individual as well as a business | accepted | monetisation, or crossing a state threshold, either of which reopens the whole United States analysis |
| D-108 | Article 20 portability applies because the basis is consent, and its scope splits on the WP242 rev.01 observed-versus-derived line. Raw token counts and their intervals, account and handle, linked provider identifiers, device registrations, participant-created social edges, board memberships and the local audit ledger are portable. Computed rank, percentile, generation and snapshot identifiers, Estimated Cash Burn, the ADR-020 confidence weight and verifier appraisal dimensions are derived and are not portable; they remain accessible under Article 15, which has no derived-data carve-out. Article 20(2) direct transmission is not offered because no interoperable recipient exists | accepted | an interoperable recipient appears, or an authority holds that a figure classified here as derived is observed |
| D-109 | Four legal preconditions are unmet release gates and are recorded as unmet rather than assumed: counsel review of `PRIVACY.md`, `TERMS.md`, `docs/privacy/DATA_MAP.md` and ADR-009; a data protection impact assessment, which is mandatory under WP248 rev.01 and under entry 7 of the German Datenschutzkonferenz list for a public rating portal scoring named individuals; the controller's legal name, postal address, supervisory authority and governing jurisdiction, which are unfilled placeholders in the participant-facing documents; and a named sub-processor list, without which an Article 15(1)(c) request cannot be answered accurately. None may be presented as satisfied | accepted | each gate closes individually when its evidence exists; the decision is superseded when all four are closed |
| D-140 | `packages/schemas/openapi-v1.yaml` is YAML. PF-065 offered two repairs for a file whose extension disagreed with its contents — rename the path to `.json`, or convert the contents to YAML — and the contents were converted. The rename was rejected because `conformance/p1140f/semantic-findings-v1.json` and `conformance/p1140f/contract-bundles-v1.json` pin the path as a string, `validate_p1140f_authority.py` requires every pinned path to exist, and those records are owned by the P-1140F closure track rather than by an API-surface change. Readers select a YAML parser; `validate_cross_references.py` and `validate_state_vocabularies.py` were moved off `json.loads` for this document | accepted | the P-1140F records stop pinning the path, at which point renaming to `.json` becomes available and is the better answer |
| D-141 | Every API operation declares its own 4xx responses, and each one carries a `reason_code` drawn from `packages/schemas/reason-codes-v1.json`. A reason code appears in exactly one HTTP status and names exactly one registered state machine, so a client can map a failure to a lifecycle without a hidden table. `state_machine: "vibeproof-v1"` on the twenty pre-existing codes was a dangling name and is repaired to the registered machine that owns each code's aggregate. Response strings are never inlined in the OpenAPI document; the registry is the sole source | accepted | a status or machine binding is shown to be ambiguous for a code, which reopens that code's row rather than the rule |
| D-142 | The idempotency contract is scoped, durable and typed: an `Idempotency-Key` is unique per `(principal, operation)` rather than per account, a stored request digest decides replay against conflict, the recorded response is returned byte-identical on replay, and the `conflict` and `expired` states of the `idempotency-ledger` machine are representable on the wire as `409` and `410`. This is the API half of SR-012 and does not close it: the persistence half lives in `packages/schemas/planning-schema.sql`, which this decision does not change | accepted | SR-012 closes with recorded evidence covering both halves, or a measured need for a wider replay window |
| D-143 | One evidence vocabulary crosses the API boundary: `evidence_class`, with values `hardened`, `standard` and `imported`, which are exactly the `public_state` values of `packages/schemas/evidence-profile-policy-v1.json` and exactly the public labels D-008 already fixed. The four competing spellings — the `packages/ui` `Hardened/Standard/Imported` labels, the `vibeproof-core` five-value scale, the policy file's `profile_id` values, and the API's previous absence of any — resolve to it: the internal scales stay internal and the API publishes the awarded public state. `evidence_class` is an assessment of self-report quality assigned by the server verifier. Under D-100 no provider offers an individual-account attestation path, so it is never a confirmation from a provider and no surface may present it as one | accepted | the evidence policy gains or loses a public state, or a provider ships individual-account attestation, which would add a genuinely provider-sourced class rather than relabel these |
| D-144 | Public surfaces publish `credited_token_burn` and never `token_burn_total`. ADR-020 makes the credited figure the raw figure times a server-assigned confidence weight, so publishing both lets any observer recover the weight by division and, because `evidence_class` is already public under D-008, recover the trust factor and therefore the sanction — which D-084 forbids. `token_burn_total`, `confidence_weight_hundredths` and both factors appear only on the participant's own surface. The banned field name `score` is removed from `RankEntry` in favour of the two named quantities, as ADR-020 requires | accepted | ADR-020's weighting is withdrawn, or D-084's private-sanction rule is replaced by a public integrity signal |

## Register rules

- Every material commitment receives a stable ID.
- Only allowed status values may be used.
- `provisional` and `research-required` items cannot be presented as production-proven.
- A planning decision may be complete while implementation evidence remains pending.
- `superseded` decisions remain for history and identify their replacement.
- Reopening an accepted decision requires the stated condition, an ADR where material and updates to dependent specifications, schemas and tasks.
- A completed planning validator proves only its declared structural checks; it does not override a later cross-contract audit.
| D-180 | Private beta access is by invite code issued directly by the owner. There is no public signup, no open registration and no self-service waitlist. The aggregate that was missing when this row was written now exists: the `invite-code` machine in `packages/schemas/state-machine-registry-v1.json`, the `invite_codes` and `invite_redemptions` tables in `packages/schemas/planning-schema.sql`, the `redeemInvite` operation in `packages/schemas/openapi-v1.yaml`, three reason codes in `packages/schemas/reason-codes-v1.json`, and `docs/security/PRIVATE_BETA_ADMISSION.md` as the normative owner of issuance, quota, expiry, revocation, the gate order and the guessing controls. D-280 through D-288 record the substantive choices inside it. This is a specification and no part of it is implemented | accepted | public signup is opened, which retires it; or participants gain the ability to issue invites, which introduces the chain D-284 records as absent and reopens the privacy analysis |
| D-181 | The public-by-default risk acceptance in ADR-021 is scoped to the private beta rather than withdrawn. An invite-only ring is not publication to an indefinite number of natural persons, so the Article 25(2) argument and the reasoning in Satakunnan Markkinaporssi and Satamedia v Finland both bear far less weight than they do against an open board. The analysis is unchanged and still applies in full the moment the board becomes publicly readable | accepted | the board becomes readable without an invite, which restores ADR-021's original scope and its full risk assessment |
| D-182 | Publication of the participant-facing legal set is deprioritised for the private beta at owner direction. PRIVACY.md, TERMS.md and docs/privacy/DATA_MAP.md remain committed and remain accurate, and the four unmet gates in D-109 are unchanged. Nothing in this decision satisfies a gate; it defers publication only | accepted | the private beta opens to anyone the owner has not personally invited, or a participant exercises a data subject right, either of which makes the notice obligation immediate |
| D-183 | The launch-blocking status of deterministic CBOR, CDDL and COSE with Ed25519 is reaffirmed for the private beta, and the D-012 crate bakeoff stays on the critical path rather than deferring to public launch. The evidence architecture is the product, so a beta that ships a simpler envelope would not exercise the thing being tested | accepted | the bakeoff shows no crate can satisfy the canonical profile, or the beta scope is cut |
| D-184 | The complete-core-product scope freeze in docs/planning/PRODUCT_SCOPE_FREEZE.md binds the private beta as well as public launch. All six board scopes across all six periods, friends, rivals, presence, notifications, moderation and appeals ship to the beta ring together, with country leaderboards remaining post-launch under D-052 | accepted | an explicit owner decision to stage the beta, which would supersede this row rather than amend the freeze |
| D-190 | The COSE algorithm identifier for VibeProof v1 is -19 Ed25519, not -8 EdDSA. RFC 9864 deprecates the polymorphic -8 because it required information beyond the algorithm identifier to determine the cryptographic operation, which is exactly what a cross-language corpus cannot tolerate. The identifier appeared in three places - the CDDL protected-header rules, the COSE_Key description in the protocol contract, and the signed exact-byte vectors - and all three moved together, because changing it changes every downstream signature and digest | accepted | IANA reassigns the identifier, or a successor to RFC 9864 supersedes the fully-specified registrations |
| D-191 | Canonical CBOR map ordering is RFC 8949 Section 4.2.1 Core Deterministic Encoding: keys sort by the bytewise lexicographic order of their encoded form with no length preference. It is explicitly not the length-first ordering of Section 4.2.3, retained there for RFC 7049 Canonical CBOR compatibility and still the default in several libraries. RFC 9052 Section 9 binds COSE to Core. The protocol contract had specified length-first; the two orderings agree on the current protected-header maps only by coincidence, because those keys happen to sort identically under both, so the defect was latent rather than visible | accepted | RFC 9052 is superseded by a profile that binds COSE to a different ordering |
| D-192 | Ed25519 verification is pinned to ZIP-215: non-canonical A and R encodings are accepted, S must be strictly less than l, and the cofactored group equation must hold. Citing RFC 8032 pins nothing - Section 5.1.7 permits both the cofactored and cofactorless equations and Section 8.8 concedes implementations then disagree on the valid set, and FIPS 186-5 Section 7.7 repeats that permission verbatim. A cofactored verifier accepts a strictly larger set and the implication runs one way, so two conforming implementations can disagree without any round-trip test noticing. Go's crypto/ed25519 is cofactorless and does not satisfy this; selecting a ZIP-215-capable Go implementation is part of the D-012 bakeoff | accepted | a divergence corpus shows ZIP-215 unimplementable in one of the two languages, which would force the stricter alternative of cofactorless plus canonical A, R and byte-string R comparison |
| D-193 | The VibeProof profile encodes no floating-point values. RFC 8949 Section 4.2.2 leaves signed-zero normalisation, integral-value encoding, NaN payload uniformity and subnormal handling to the protocol rather than deciding them, and float determinism is the most reliable source of cross-language signature mismatch. Token Burn, Estimated Cash Burn and every other aggregate are integers with a fixed stated scale, and the canonical encoder refuses a float rather than encoding one | accepted | a quantity is shown to require a range or precision that a scaled integer cannot express |
| D-194 | The exact-byte vectors are generated from the recorded seed by scripts/repository/generate_vibeproof_vectors.py and never hand-edited. Generation is deterministic and a validation stage fails when the committed file differs from a regeneration. Hand-maintained signed fixtures drift from the profile they claim to encode, and a fixture that is internally self-consistent but wrongly constructed passes a signature check while proving nothing | accepted | the vectors move to a generator owned by the implementation rather than the planning tree |
| D-195 | Every state-machine persistence owner named in packages/schemas/state-machine-registry-v1.json resolves to a table in packages/schemas/planning-schema.sql, and validate_state_vocabularies.py fails when one does not. Nineteen of twenty-six machines named at least one table the DDL did not define, across twenty-five distinct tables, so those aggregates had no persistence owner at all despite AGENTS.md requiring exactly one. The defect was invisible because the registry stores owners in kebab-case and the DDL declares them in snake_case | accepted | persistence moves out of the planning DDL, at which point the check follows it rather than being removed |
| D-196 | State graphs are checked for reachability, sinks and false terminals across every machine rather than every bound machine. A state that no transition can reach is dead vocabulary, a non-terminal state with no outgoing transition is an unadmitted sink, and a terminal state with outgoing transitions is not terminal. All three classes were present: interactive-shell had ten unreachable states and two sinks, daemon-lifecycle four unreachable states, privileged-supervisor one, and four machines declared a terminal state that still had outgoing edges. A state named only as a transition source still looks used, which is why none of it surfaced | accepted | a machine legitimately needs an unreachable state, which would need its own recorded reason rather than a validator change |
| D-200 | Every work unit in docs/implementation/PR_SIZED_WORK_BREAKDOWN.md carries the required fields, at owner direction. The document previously argued the opposite - that expanding 195 unspecified units before any was exercised would repeat the failure being repaired - and that argument is retained in the file rather than deleted, because the risk it names is real. The judgement that changed is which failure costs more: an unspecified unit hid five defects the document had already recorded against itself and could not act on, because a heading offers a validator nothing to check. Expansion is scoped to what it can claim: a path that does not exist carries the (new) marker and is an intention, and two units whose real acceptance is a human review say so instead of substituting a green ledger for a verdict | accepted | a specified unit is found to have been specified wrongly at a rate that makes the file misleading rather than useful, which would argue for narrowing the standard rather than for returning to headings |
| D-201 | Work-unit status is a recorded field checked against the tree rather than a claim. Each unit's (new) paths are the artifacts it promised to create, so not-started fails once any of them exists and landed fails unless all of them exist and the status names a resolvable commit. in-progress is deliberately unconstrained because artifact presence cannot refute it, and is counted separately for that reason. A unit that promises no new file gives the check nothing to observe, and that count is reported as its own number so a green run is not read as more coverage than it has. The what-to-start-next list is generated from the statuses and the graph, because the hand-written one still named PF-053 as unstarted after its ADR was committed | accepted | status gains a meaning artifact presence cannot approximate, at which point the check follows the new meaning rather than being relaxed |
| D-202 | Every table in packages/schemas/planning-schema.sql must be named by a work unit, and validate_work_unit_status.py fails when one is not. The recorded defect counted 52 unowned tables out of 73; the DDL declares 98, so 77 were unowned. A table with no owning unit is persistence nobody is scheduled to build, and it is invisible precisely because a table and a plan are different files | accepted | persistence moves out of the planning DDL, at which point the check follows it rather than being removed |
| D-203 | P-1105 launch readiness gates on an explicit unit list rather than on a transitive accident. X-011 previously reached 162 of 194 units and excluded every Epic W unit, so launch readiness was declarable with no web application. Its Depends line now names every epic-terminal unit, every previously excluded unit and the six new operations units, and validate_work_unit_status.py --gate X-011 fails while any unit in the closure is not landed. The closure is 216 of 245 units; the 28 outside it are all PF-037 through PF-067, planning repairs whose value is repository hygiene rather than a launch prerequisite. The mechanical gate is not the verdict: readiness remains an owner decision and no agent may derive it from a green run | accepted | a unit is added that belongs in the launch set, which extends the list rather than reopening the rule |
| D-204 | A prose dependency is replaced by unit IDs, and the condition the phrase was also carrying is recorded as a condition rather than as an edge. Six units depended on phrases such as implemented product paths and all launch paths. In four of them the phrase carried two things at once: an ordering claim, which becomes a Depends entry, and an authorization or capability condition - separate deployment authorization, separate privilege review, never a CI default - which is not an edge and cannot be scheduled. Collapsing the two is what made the phrases look like dependencies | accepted | a condition is found that genuinely orders work, which would make it a unit rather than a note |
| D-205 | The thirteen categories with no work unit are authored as units, and product analytics among them is server-side aggregate only. The binding product rules admit only fixed-schema aggregate accounting across the device boundary and forbid content-derived data, so there is no behavioural event pipeline to build and X-017 does not propose one; its acceptance requires the N-020 egress suite to pass unchanged, which is what proves no client-side stream was added. F-010 product CI/CD and P-010 fuzz activation remain outside the current authorization and say so in the unit; specifying them is not activating them | accepted | a product decision is judged to need behavioural analytics, which requires a decision changing the egress rule rather than a wider collector added under an analytics heading |
| D-210 | An Article 17 erasure is a cryptographic tombstone plus key destruction. Ranking entries are keyed on an erasure domain — an opaque pseudonym, one per ranked-identity lineage — and the only stored binding between that pseudonym and the person is an AES-256-GCM ciphertext of `account_id ‖ lineage_id` under a key that encrypts nothing else. The erasure destroys that key, appends a hash-chained COSE_Sign1-signed record to `erasure_records`, and deletes every live personal record including accepted claims, in one serializable transaction. Rebuild under a new generation identifier was rejected: it multiplies storage by the number of erasures, renumbers positions and therefore breaks every held cursor, and does not erase anything unless the old generation is also deleted, which is the deletion the immutability rule forbids. `docs/privacy/ERASURE_AND_KEY_DESTRUCTION.md` is the mechanism and ADR-022 is the reasoning | accepted | the D-085 reopen conditions, which are the same conditions |
| D-211 | A sealed ranking generation is written once and never modified. `content_hash` covers the entry rows in position order and covers no handle, no account identifier and no appraisal detail, so a rename, a block, a visibility change and an erasure all leave it and the `snapshot_id` it names intact. `sealed_entry_count` and `sealed_total_credited_token_burn` are frozen at seal and never recomputed, because republishing a recomputed aggregate for one generation lets an observer difference the two publications and recover the exact figure of a participant who has since been erased. An erased entry is retained for aggregate integrity and renders nothing at all — no handle, no figure, no placeholder — because a figure at a retained position joins trivially against a third party's archived copy of the board, and a visible marker publishes the fact of the erasure against a position that archive already names | accepted | an aggregate integrity requirement that a frozen count cannot satisfy, or evidence that a rendered placeholder is materially better understood than an absence |
| D-212 | A durable leaderboard cursor anchors on `(ranking_view_id, generation, position)` and carries no subject identifier, no handle and no figure. Positions inside a sealed generation are immutable, so a client holding a stale cursor whose anchor entry was erased receives the next page from that position, with the erased entry absent and every surviving entry where it always was: no restart, no renumbering, no error. A superseded generation is still readable through the cursor that names it and the response reports both generations rather than jumping. The only restart outcomes are a purged ranking view and a changed filter, which is a different `ranking_view_id`. The previous form embedded an account identifier and a score in a token the client keeps indefinitely, which was a personal-data leak into client storage independent of erasure | accepted | a ranking function that makes positions mutable inside a sealed generation, which would require its own decision first |
| D-213 | Key material is scoped, single-purpose and destroyed rather than rotated. One AES-256-GCM key per erasure domain, encrypting exactly one 32-byte plaintext, so an erasure's blast radius is exactly the erasure. Rotation is not scheduled: rotating while keeping the old key alive changes nothing and rotating while destroying it is an erasure, so the key has two states and they are a check constraint — material present exactly when a destruction time is absent — rather than a vocabulary a worker must advance correctly. Destruction is proved by a retained `key_commitment`, SHA-256 over `key_id ‖ key_material` taken before destruction, which tests a candidate key and cannot be inverted. The account-to-domain lookup is an HMAC under a separate server-wide key and is not destroyed, because its preimage is a 128-bit identifier the erasure deletes; UUIDv7 leaves 74 unguessable bits, which is stated rather than implied. Co-locating the keyring with the ciphertext gives no confidentiality at all: this is a deletion control and never encryption at rest | accepted | a keyring topology that becomes affordable within the D-093 ceiling, which changes where the key lives without changing what destruction means |
| D-214 | Point-in-time recovery cannot exclude a row, so every erasure record is mirrored before acknowledgement to an append-only journal held outside the database backup set, and a restore is not complete until the journal has been replayed. The reapply re-destroys keys, re-deletes live rows, re-applies tombstones, verifies, and writes an `erasure_restore_receipts` row whose check constraint makes a receipt admitting traffic before the reapply completed unrepresentable. The journal is retained one day longer than the backup window, because after the backup window no restore can resurrect anything it would need to re-erase. **The window in which a restore can resurrect an identifier is from restore completion to reapply verification**: the cluster serves no application traffic during it, an operator with credentials can read it, it is bounded by the replay, and it is not zero. A restore performed without the reapply, or a copy taken during the window, is outside the mechanism entirely. Beyond the 35-day backup window no restore can resurrect anything | accepted | a managed recovery product that can exclude a relation from point-in-time recovery, which would make the journal a redundancy rather than the mechanism |
| D-215 | `minute_scores`, `notifications` and `audit_events` are range-partitioned and their retention windows are enforced by dropping partitions. `claims` is **not** partitioned, and the earlier contract statement that it was partitioned by receipt month was not executable: PostgreSQL requires every unique constraint on a partitioned table to include the partition key, `claims` carries three global uniqueness invariants the acceptance transaction depends on, and six tables hold a foreign key to `claim_id`, which must reference a unique constraint. `outbox_events` and `social_integrity_events` are unpartitioned for the same reason. Every foreign key gains an index on its referencing side, which PostgreSQL does not create, and every documented query path gains the index it needs; each is built `CONCURRENTLY` outside a transaction, which is what D-097 selected a migration tool for | accepted | measured evidence that a partition boundary or an index choice is wrong, which is a benchmark result rather than an argument, and none has been run |
| D-216 | `packages/schemas/data-disposition-v1.json` is the machine-readable retention and disposition authority, with one row for every persistence owner in `packages/schemas/planning-schema.sql` stating classification, retention window, erasure action, backup-set membership and enforcement owner, and naming the actor for every `expires_at` column. Windows are either a key in `policy-defaults-v1.json` or a named rule tied to another aggregate's life; `docs/privacy/DATA_MAP.md` remains the Article 30 record and the prose authority, and the registry is the executable form of the windows it states. A validator fails when a table is missing, when a policy key does not resolve, when an `expires_at` column has no actor, or when the journal window is not exactly one day beyond the backup window | accepted | the Article 30 record and the registry disagree, which is a defect in whichever one moved without the other rather than a reason to remove the check |
| D-217 | The binding rule that Token Burn is the default raw ranking metric and the product specification's permission for profiles to expose Token Burn are reconciled with ADR-020 rather than left in contradiction. Token Burn remains the raw ranking metric of record — accepted, immutable, unnormalized — and public rank is computed on Credited Token Burn. Public surfaces publish the credited figure only; the raw figure is confined to the participant's own surface and to viewers they authorize, because the two published together yield the composite weight by division and, since the evidence profile is already public under D-008, yield the trust factor and therefore the sanction D-084 keeps private. The residual leak is recorded and not hidden: an observer differencing a published credited series across periods sees a discontinuity a trust-state change explains and no evidence-profile change does. Weighting whose output is visible cannot prevent that; what survives is deniability, since there is no marker and an ordinary drop in activity looks identical | accepted | ADR-020's weighting is withdrawn, or D-084's private-sanction rule is replaced by a public integrity signal, either of which removes the reason for the narrowing |
| D-218 | `score` is removed as a column name. `minute_scores.score` becomes `token_burn_total`; `period_scores.score` becomes `token_burn_total`, `confidence_weight_hundredths` and `credited_token_burn`; `ranking_corrections.delta` becomes `token_burn_total_delta`. The three table names still contain the word, and that is a deliberate stop rather than an omission: `packages/schemas/state-machine-registry-v1.json` names `period-scores` and `score-snapshots` as persistence owners of the `ranking-projection` machine, `validate_state_vocabularies.py` fails when a named owner does not resolve to a table, and that registry belongs to the P-1140F closure track. On the API surface the field is already gone: D-227 removed `RankEntry.score` in favour of `credited_token_burn` and put `token_burn_total` and both weight factors on `SelfRankEntry` alone, which is the same raw-versus-credited split this decision applies to the columns | accepted | the registry and the OpenAPI document are edited by their owning track, at which point the table names and the response field follow |
| D-220 | Session authentication reaches the API surface exactly as ADR-015 states it, so the edit is transcription rather than interpretation. The document-level `security` block is removed and every operation declares its own requirement. `bearerAuth` is retained unchanged; `sessionCookie` (`__Host-vm_session`), `refreshCookie` (`__Host-vm_refresh`), `csrfToken` and `deviceProof` are added. A native requirement names `bearerAuth` and `deviceProof` in one requirement object so neither satisfies an operation alone, a cookie-authenticated state-changing operation names `sessionCookie` and `csrfToken` together, and a public operation declares the empty requirement explicitly rather than inheriting one. The normative OAuth citation is RFC 9700; "OAuth 2.1" is cited nowhere, because `draft-ietf-oauth-v2-1` is an Internet-Draft and a normative contract that cites one cites a moving target. RFC 9207 issuer validation fails closed through `OAUTH_ISSUER_MISMATCH` at `completeGitHubAuth` and `completeXAuth` | accepted | RFC 9700 is obsoleted or materially updated, or `draft-ietf-oauth-v2-1` reaches RFC status, at which point the citation moves and the requirements are rechecked against the published text |
| D-221 | `refreshSession` and `revokeAllSessions` exist, which closes the two operations ADR-015 names and the contract lacked. `refreshSession` carries no `Idempotency-Key` and is added to the idempotency exception set in `scripts/repository/validate_planning_coverage.py` and `scripts/repository/validate_planning_artifacts.py`, because the refresh handle it presents is already one-time-use with no grace window: a durable request hash over a single-use credential is a second and weaker replay control layered over a stronger one, and it would make a repeated refresh look like a retry when ADR-015 makes it an incident. Its `x-idempotency` value is `single-use-refresh-handle`, which names the control that actually applies | accepted | a measured rate of daemon reauthentication prompts high enough to damage collection continuity, which ADR-015 answers with a durable client-side write rather than a grace window |
| D-222 | Pagination has one style and every operation returning a collection uses it. The `cursor` and `limit` parameters are declared by all seventeen operations whose success response is a page schema; thirteen gained them here. Twelve are the operations PF-044 names and the thirteenth is `getPresence`, which returns `PresencePage` and was missing from that list. The `cursor` is opaque, server-issued, bound to the `snapshot_id` it was issued against and to the principal it was issued to; a client neither constructs nor parses one. No second pagination style is introduced, and the existing page shape — `items` with `maxItems` 200, `cursor` with `maxLength` 2048, `snapshot_id`, `revision` — is unchanged | accepted | a collection is shown to need a stable offset rather than a cursor, which would need its own decision rather than a second style added quietly |
| D-223 | The operation-to-status-to-reason-code matrix lives in `packages/schemas/reason-codes-v1.json` and not in `packages/schemas/openapi-v1.yaml`. A code binds to operations through eleven operation classes — `all`, `public`, `authenticated`, `native-bound`, `mutating`, `mutating-authenticated`, `recent-auth`, `idempotent-durable`, `collection`, `path-addressed`, `request-body` — each of which is derived from the OpenAPI document rather than hand-listed, plus an explicit operation list where a code binds to named operations. `scripts/repository/validate_planning_artifacts.py` derives the classes, fails when the registry's recorded membership differs, and fails when an operation declares a 4xx status no code claims or omits one a code requires. This refines D-141: a code names exactly one registered authority, and `server-runtime` is the authority for transport-level codes that report no aggregate lifecycle, which is the authority set the P-1140E contract validator already admits | accepted | a code is shown to need two statuses, which reopens that code's row rather than the rule |
| D-224 | D-141's repair of the twenty pre-existing `state_machine: "vibeproof-v1"` values is completed only in part, and the remainder is recorded rather than faked. `DEVICE_STATE_CLONE_SUSPECTED` and `DEVICE_REVOKED` move from the unregistered `device-lineage-v1` to the registered `device-enrollment` machine. The fourteen claim-level codes and `BATCH_ATOMIC_REJECT` keep `vibeproof-v1`, because no `claim-record` machine exists in `packages/schemas/state-machine-registry-v1.json` and that file is not changed here; naming a machine that does not own the claim aggregate would be a worse defect than naming the protocol. Those fifteen codes are also given `transport` values, so the registry now says which ones never reach an HTTP status at all: fourteen are per-claim rejections inside a `ClaimBatchResult`, and `ACCOUNT_RANKING_RESTRICTED` and `MODERATION_HUMAN_REVIEW_REQUIRED` are appraisal outcomes read through the private notice path | accepted | a claim-record state machine is registered, at which point the fifteen codes rebind to it |
| D-225 | The idempotency contract is stated on the wire under one root `x-idempotency-contract` block: the key is scoped to `(principal_id, operation_id, idempotency_key)`, the principal is the account the session resolves to plus the bound device enrollment for a native session, a SHA-256 request digest decides replay against conflict, a committed row records the full response so a replay returns it byte-identically under an `Idempotency-Replayed` header, a `conflict` row answers 409 with `IDEMPOTENCY_KEY_CONFLICT`, an `expired` row answers 410 with `IDEMPOTENCY_RECORD_EXPIRED` after a 168-hour retention window, and a `reserved` row that never committed moves to `failed` on lease expiry so the key becomes usable again. This is the API half of SR-012 and does not close it: the nullable `response_digest`, the absent response-body column and the account-only primary key live in `packages/schemas/planning-schema.sql`, which this decision does not change | accepted | SR-012 closes with recorded evidence covering both halves, or a measured need for a retention window other than 168 hours |
| D-226 | `evidence_class` crosses the API boundary on `PublicProfile`, `RankEntry`, `AccountProfile` and `ClaimRecord`, with the three values D-143 fixed. The disclosure projection is carried by the response shapes themselves rather than by a separate schema file: `PublicProfile` and `RankEntry` are what an authorized viewer receives, `SelfRankEntry` is what the participant receives about themselves, and the difference between them is the projection. The separate disclosure schema PF-046 names is not authored here, so PF-046 is partial. Every description of the class says it assesses the quality of a self-report and none presents it as a confirmation from a provider, because under D-100 no provider offers an individual-account attestation path | accepted | a provider ships individual-account attestation, or the disclosure projection needs a viewer-by-viewer rule that a response shape cannot express |
| D-227 | The profile and rank shapes carry what the design system renders. `RankEntry` loses `score` and gains `credited_token_burn`, `display_name`, `avatar_id`, `evidence_class`, `previous_position`, `rank_movement`, `availability` and `top_model_alias`; `PublicProfile` gains the same public fields plus `friend_count` and `board_count`; `AccountProfile` gains `visibility`. `SelfRankEntry` is added for `getCurrentRank` and is the only shape carrying `token_burn_total`, `confidence_weight_hundredths` and both factors, which is ADR-020's visibility rule expressed as two schemas rather than as a comment. `avatar_id` indexes the repository-owned avatar library because the product accepts no uploaded image. Two limits are recorded rather than hidden: `display_name` has no column in `packages/schemas/planning-schema.sql`, which this decision does not change, and the participant's own trust state is reachable only as `trust_notice_id` rather than by name, because the ranked-identity states ADR-020 reads are marked internal in the binding table that `docs/architecture/AUTHORITATIVE_STATE_AND_PLATFORM_CONTRACT.md` owns | accepted | the profile persistence unit adds the display-name column, or the internal-state marking is revisited so a participant can read their own trust state by name |
| D-228 | The four banned claim strings in `packages/ui/src/concepts/product-storyboards.tsx` are replaced rather than softened. "Verified competitor" becomes the awarded evidence class, "All sources verified" becomes "All sources reconciled", "clean, verified agent activity" becomes recorded agent activity, and "Rankings are based on verified Token Burn" becomes Credited Token Burn, which is also what ADR-020 makes true. Only copy strings change; no component logic and no styling is touched | accepted | the evidence vocabulary changes, which moves the copy with it |
| D-230 | Cross-origin access to the public API is an exact allowlist of three origins: the hosted web application, its canonical www redirect target, and a localhost development origin compiled out of the production build rather than configured off. No wildcard, no reflected origin, no subdomain wildcard, no third-party or embedding origin. Every state-changing browser request requires all three of an allowlisted Origin, a session-bound double-submitted CSRF token, and the SameSite=Lax cookie ADR-015 sets; bearer-authenticated native requests are exempt from the first two because a cross-site page cannot attach a credential it does not hold. Before this row the repository contained no occurrence of cors, Access-Control or cross-origin anywhere, so the ADR-015 requirement that an Origin match an exact allowlist named no allowlist and had no implementable form. docs/security/ORIGIN_AND_LOOPBACK_CONTROLS.md owns it | accepted | a second first-party surface on a different origin, or an embedding partner, either of which extends the list rather than wildcarding it |
| D-231 | Every loopback HTTP listener, meaning the local dashboard and the adapter-one OTLP receiver, applies a Host header allowlist, an Origin rule for state-changing requests, a session token that is never held in a cookie, a random high port recorded in local state, bind verification before listening, no CORS headers at all, a 900-second idle and 3600-second absolute token expiry, and a 10-per-minute unauthenticated probe limit. The Host allowlist is the control that defeats DNS rebinding, because a rebound request still carries the attacker's name in Host while resolving to a loopback address. Loopback binding is a network control and a browser on the participant's own machine is not on the network, so binding alone was never a boundary against the one adversary that matters here; DNS rebinding appeared nowhere in the repository before this row | accepted | a platform stops exposing the Host header to the listener, or a loopback surface acquires a legitimate cross-origin consumer, which would need its own ADR |
| D-232 | Rate limits are numeric, per operation class and per principal class, derived from the load 200 private-beta participants generate rather than chosen: 60 per minute public read, 120 per minute authenticated read, 6 per minute claim ingest, 4 per minute presence, 30 per hour social mutation, 5 per day board creation, 10 per hour authentication start, a 5-second minimum device-poll interval, 5 per day subject-rights requests, and 20 per day moderation reports, under a 200 requests-per-second aggregate admission ceiling. Ordinary limits are published in headers; adaptive limits applied to a principal under suspicion emit nothing and are indistinguishable on the wire. Subject-rights requests are throttled but the first request in any 24-hour window is always admitted, because Article 12(5) of Regulation (EU) 2016/679 permits refusal only for manifestly unfounded or excessive requests. The values live in packages/schemas/policy-defaults-v1.json and docs/architecture/API_EDGE_CONTRACT.md states their derivation | accepted | measured traffic shows a limit binding on legitimate use, or the participant population moves off the 200-participant planning figure |
| D-233 | Clients retry with exponential backoff and full jitter, a uniform draw over the whole interval, base 1 second and cap 60 seconds; they honour Retry-After whenever it is larger, hold a retry budget of 10% of successful requests over a trailing 60-second window with a floor of 3, and open a circuit after 20 consecutive failures for 60 seconds. Attempt ceilings are 3 interactive, 5 native read, 6 native keyed mutation, and unbounded-with-capped-delay for the daemon claim queue, which enters offline after 24 hours because dropping a queued claim loses activity the append-only ledger cannot recreate. Retry safety is decided by the specification and never by the HTTP method: every GET, keyed mutations under the idempotency contract, and nothing else. That safety is currently partial, because SR-012 records that the schema stores only a response digest, so until PF-049 lands a retry is safe against duplicate side effects and is not safe against receiving a different response body than the original, and this row does not claim otherwise | accepted | PF-049 closes, which upgrades the keyed-mutation guarantee; or measured client behaviour shows the budget or the ceilings are wrong |
| D-234 | The API is versioned in the URL path, one version, with no version header and no content negotiation, because the release-set model already binds a client build to a supported version range and a path version makes that binding visible in a log line and a proxy rule. Within a major version only additive changes are permitted; adding a member to a closed state vocabulary is a major-version change; clients must ignore unknown fields and unknown reason codes. A deprecated surface carries Deprecation, Sunset and a Link relation with a minimum 180-day window, which is two quarters and therefore long enough that a participant who opens the application once a quarter still receives a build that has stopped using the surface before it disappears. Three carve-outs shorten it: a security removal on the ADR-013 emergency deadline, a surface no released build has ever called, and a legally compelled removal | accepted | a breaking change is genuinely unavoidable, which opens a second major version under the same window rather than amending this row |
| D-235 | Clock discipline has numbers. A participant's machine must be within 300 seconds of the server, which is simultaneously the ADR-015 request-signing bound, the future-event tolerance and the declared-uncertainty ceiling, deliberately one number because a client that cannot authenticate cannot submit and a wider admission window would create a band that is admissible but unpresentable. Five minutes is 0.35% of the shortest competitive period, the UTC calendar day, so a maximally skewed honest clock lands a negligible share of claims on the wrong side of a boundary. The server reviews its own measured offset at 250 ms and refuses to finalize claims or assign periods beyond 2000 ms, an order of magnitude apart and roughly an hour of unsynchronized virtual-machine drift. The host clock is disciplined by a network time protocol client with at least four sources; on managed compute the provider owns that discipline and its published statement is an ADR-017 selection input, but the service measures its own offset independently, because an assurance it cannot verify is not one it may rely on for an irreversible period assignment. No interval is computed by subtracting two wall-clock timestamps, so leap-second handling changes no accepted total | accepted | a measured population shows the 300-second bound excludes legitimate machines, or a period shorter than a day becomes competitive |
| D-236 | Application logging is JSON lines on standard output, using Go log/slog, Rust tracing and a thin TypeScript wrapper, at four levels defined by operator action rather than by severity feel, with a client error never logged as ERROR and DEBUG off in production and not enablable remotely. Every line carries a timestamp, level, message, service name, build version, deployment environment and a server-generated UUIDv7 request identifier that is never accepted from a client. The message is a static literal and never interpolated, because interpolation is the commonest route by which forbidden content reaches a log and a field allowlist cannot see inside a formatted message. Principals appear as a truncated HMAC under a per-environment salt rotating every 90 days, so an operator can follow one principal within an epoch and no corpus joins across epochs, and the mapping is not stored. Operational and security-audit streams are separate and a network address appears only in the second. The observability allowlist is extended to govern structured log fields as well as metric and span attributes, which nothing previously did | accepted | never silently reopen the forbidden list; a log product that cannot ingest JSON lines would change the transport and not the fields |
| D-237 | The emitted signal set is enumerated: eighteen named metrics covering request duration, rate-limit rejections, claim acceptance, rejection, duplication and batch size, outbox depth and age, aggregate freshness, ranking duration, database duration and pool usage, OAuth exchanges, presence leases, notification delivery, the privacy canary, update blocking and backup age. Spans exist at four boundaries and are logged but never exported, because there is no tracing backend: a hosted one is a recurring cost against D-093 and would have to process inside the European Union under ADR-017. Emitting the trace and span identifiers anyway costs nothing and makes adopting a backend later a configuration change rather than an instrumentation project. The privacy-canary metric is the one signal whose correct value is a constant, and any non-zero reading is the operations contract's highest severity. Readiness returns success only when the applied migration version is inside the binary's declared range, which is the ADR-018 version gate, and neither health endpoint returns any detail beyond a status code | accepted | a metric is found to be unactionable, which removes it, or a funded tracing backend changes the export decision |
| D-238 | Four environments exist rather than five: local, ci, preview and production. The environment previously called test is renamed ci; preview is a static per-pull-request web build with no database and no API, because a per-pull-request managed PostgreSQL instance is the whole budget several times over; and staging does not exist and is not funded. The ADR-018 preproduction restore drill and the quarterly disaster-recovery exercise therefore run in an ephemeral ci database restored from a production backup rather than in a standing environment. The consequence is recorded rather than absorbed: no build is observed under realistic load before it reaches participants, and under D-180 the invite-only beta ring is itself the pre-production population. Production deployment is manually dispatched against a tagged release set and never automatic, because under D-092 an automatic deployment is a deployment into an unattended service | accepted | a second maintainer under D-091 or a budget above the D-093 ceiling, either of which makes a standing staging environment affordable |
| D-239 | Secrets have a mechanism and a per-class cadence: OAuth client secret 365 days, session signing key 90 days with two generations retained for verification because the ADR-015 web refresh family caps at 90 days absolute, device-enrollment key 180 days, database credential 180 days, backup encryption key 365 days with old keys retained for the full backup retention window, observability ingest key 180 days, log pseudonymisation salt 90 days, TUF timestamp 1 day, snapshot 7 days, targets 90 days, root 365 days re-signed at 270, and release signing key 730 days. No long-lived cloud CI credential exists; CI uses workload-identity tokens under one hour. Any secret rotates immediately, outside its cadence, on a maintainer change, a suspected exposure, a provider breach notice, a lost custody device, or a history scan finding it. Dual control for root and release keys is required by the operations contract and is unsatisfiable under D-091: what exists instead is a single-operator checklist with a signed usage record, which detects an error the operator notices afterwards and prevents nothing, and it is recorded as weaker rather than described as equivalent | accepted | a second maintainer joins, which is the same trigger that withdraws the D-091 administrator bypass and makes dual control real |
| D-240 | Test strategy is a placement rule rather than a pyramid ratio, because a ratio target produces tests written to move a ratio: a test lives at the lowest level that can observe what it asserts, and the only four boundaries that justify an integration test are PostgreSQL, HTTP, local IPC and the filesystem. Business logic is never tested through HTTP and SQL is never tested through a mock. Browser journeys are capped at twelve by the ten-minute pull-request budget. Coverage floors are 90% on protocol crates, 85% on Go accounting and ranking, 75% on Go boundary code, 70% on the UI package, 60% on web route code, with generated code excluded, under the same non-regression rule the coverage baseline already applies to lanes and a one percentage-point band. Frameworks are the ones ADR-002 already names or the repository already pins; nothing new is introduced without that provenance. A test failing twice in 30 days on unchanged code is quarantined within 7 days and fixed or deleted within 30, and deleting it is an acceptable outcome | accepted | a measured coverage baseline shows a floor is unreachable for a structural reason, which moves the floor with the reason recorded |
| D-241 | Six load scenarios are specified with derived loads and readable pass conditions: beta-steady at the 20 requests per second that 200 participants generate, beta-peak at ten times that, offline-drain with 50 devices each carrying a 24-hour backlog, leaderboard-thunder with 2000 anonymous readers arriving inside a minute, ratelimit-breach with one principal at ten times its class limit alongside steady load, and a 12-hour soak. They run before each beta ring expansion and before public launch rather than per pull request, against a ci-provisioned ephemeral stack. k6 is the runner, and its AGPL-3.0 licence is acceptable because it is invoked as a separate process and is never linked into or distributed with a product artifact. These scenarios say nothing about the 100,000 ranked-identity scale target and passing them may never be cited as evidence for it; D-094 owns that conflict, and a scenario set at that scale is deliberately unspecified because there is no budget line to run one | accepted | the participant population moves materially off 200, or D-094 resolves and a funded scale test becomes possible |
| D-242 | Conformance suites get a directory contract: a README, and a manifest naming authorities, subjects, and cases carrying fixture SHA-256 digests, an expected outcome, an expected reason code, and a reference to the specific clause each case enforces, with at least one negative case per suite or the manifest fails validation. Runners are generic over the manifest rather than bespoke per suite, and a case passes only when every declared subject matches the expectation and the subjects match each other; the harness distinguishes a shared wrong answer from a divergence, because those need different responses and AGENTS.md already states that cross-language agreement is not conformance when both implementations consume the wrong authority. Case identifiers are permanent and never reused. Manifest validation can run today against fixtures no runner executes, and passing it is explicitly not conformance and changes no suite status | accepted | a suite is found to need per-suite runner logic the manifest cannot express, which would be a manifest defect before it is a design defect |
| D-243 | The seven proposed service level objectives are restated rather than retained. Four commitments remain, each met by a mechanism that runs whether or not anyone is awake: zero forbidden content across the device boundary, a 100% deterministic duplicate and replay fixture pass every release, accepted claims never silently altered, and evidence status assigned only by the server verifier. Everything else becomes an observation with a review trigger and no response time: monthly availability reviewed below 99.0% rather than committed at 99.9%, ingestion p95 above 1000 ms, leaderboard p95 above 600 ms, freshness above 300 seconds, backup age above 26 hours, and monthly spend above 80 USD. Alerts have three classes, immediate, daily-digest and ticket, with no pager, no rotation and no acknowledgement requirement, and each immediate condition has one containment action executable from a phone. The recovery position is stated honestly: at a daily snapshot the recovery point objective is 24 hours, at which the operations contract's no-loss line does not hold, and D-094 owns that conflict unresolved | accepted | a paid tier, a second maintainer or an external obligation creates a real availability commitment, which is the same reopen condition as D-092 |
| D-244 | Local development is specified as three layers: planning validation with no containers, a PostgreSQL container for the DDL stage the validators skip without a database URL, and a product stack that does not exist yet. The compose definition is written out in full in docs/engineering/LOCAL_DEVELOPMENT.md rather than committed, because committing infrastructure now adds a product artifact the current phase forbids and the file belongs with the first service that needs it. Three properties of that definition are load-bearing: the port binds to the loopback address explicitly rather than to every interface, the database initialises with the C locale so that an ordering cannot differ between two engineers' operating systems and a leaderboard is an ordering, and the volume is named so a routine teardown does not discard a database mid-debug. Local development holds no credential valid anywhere else, uses a stub authorization server against synthetic accounts, and has no shared development credential, because a shared credential is one whose compromise nobody notices | accepted | the first service lands, which converts the compose definition and the make targets from a specification into files |
| D-245 | Two reason-code gaps that the D-235 clock bounds depend on remain open and are recorded rather than worked around. RATE_LIMIT_EXCEEDED now exists at registry version 1.3.0 under D-223, so the rate-limit half of this gap closed before this row was written and the 429 contract is implementable. What does not exist is a code for a clock rollback or for a future timestamp: conformance/adversarial/anti-cheat-registry-v1.json maps clock-rollback onto CLAIM_SEQUENCE_UNEXPECTED, which describes a different fault and would make a rollback indistinguishable from an ordinary sequence gap in every appeal and every metric. Registering one is blocked behind the partial repair D-224 records, because fifteen codes still name a vibeproof-v1 state machine that is not registered and a new code would either join them or sit inconsistently beside them | research-required | D-224's remaining state-machine references are repaired, after which a clock-rollback and a future-timestamp code are registered together and this row becomes accepted |
| D-206 | A landed work unit is backed by executable evidence, never by a commit id. The first revision of the status field recorded Status: landed <sha> and it was unfalsifiable in both directions: every pull request is squash-merged under a linear-history rule, so the ids recorded from a branch stop existing when it merges and the check failed on correct data, while any resolvable id would have satisfied it including one belonging to an unrelated commit. A commit id describes nothing about whether the work is present. Each landed unit now carries one or more Evidence lines in a five-verb shell-free form - validator, unittest, exists, missing, contains and absent - and validate_work_unit_status.py runs them on every check. Where completion genuinely cannot be observed from the tree the status is unverifiable with a recorded reason, never landed | accepted | an evidence verb is shown to admit an assertion that passes without the work being present, which narrows the verb rather than restoring the commit id |
| D-207 | The OS-001 through OS-013 units proposed in https://github.com/vedant-simulacrum/vibemaxxing/pull/66 are absorbed with their identifiers unchanged, and the six units this branch had authored over the same surfaces are marked superseded-by rather than deleted. Renaming a published identifier breaks every reference to it, and deleting the duplicates would erase the record that two branches specified the same surface independently. The OS unit wins each pair because a normative owner document now exists behind it and carries the numbers. validate_work_unit_status.py fails when any unit depends on a superseded one, because a superseded unit will never land and anything waiting on it would wait forever | accepted | a superseded unit is found to cover something its successor does not, which reopens that pair rather than the rule |
| D-260 | Token Burn arithmetic is defined by packages/schemas/accounting-arithmetic-v1.json rather than by whichever implementation reads a profile first. Quantities are unsigned 64-bit integers carried as decimal strings; addition and subtraction are checked and a result outside the domain rejects rather than saturating or wrapping, because both alternatives return a plausible-looking number for an impossible one. Containment is resolved container-before-member with a checked subtraction, and a member larger than its container, a member with two containers, a containment cycle, or a source field the profile does not declare all reject. Summation follows the fixed canonical component order, because addition over a bounded checked domain is not associative with respect to failure and a different order fails at a different step | accepted | a source appears whose categories cannot be expressed as a containment forest, which would need a new resolution rule rather than a new order |
| D-261 | The canonical digest of a planning policy record - accounting profile, accounting arithmetic, appraisal policy bundle, observer-equivalence rule, producer binding - is SHA-256 over its RFC 8949 core deterministic CBOR encoding with content_sha256 omitted. accounting_profile_sha256 is a signed claim field, so leaving the preimage to an implementation's JSON serializer meant two conforming implementations could sign different digests of the same profile. The preimage admits integers, text strings, arrays, maps and null, and refuses floats under D-193 and booleans because a two-valued field written as a named enum stays legible inside the digest. The three registered accounting profiles carried placeholder digests of repeated hex digits until this decision, so the signed field bound nothing | accepted | a record needs a type the profile refuses, which would need its encoding decided here rather than at the call site |
| D-262 | The retry, cancellation and nested-execution enums in packages/schemas/accounting-profile.schema.json have defined behaviour rather than labels. Each value now names which attempts count, whether a distinct model execution is required, what identifies an attempt, and which control prevents the double count. Cancellation states the counted quantity and what happens to an unknown remainder, which is absent and never zero. Nested execution states child attribution, whether a parent total contains its children, and which control deduplicates. An enum whose behaviour is unwritten is a field two implementations agree on the spelling of and nothing else | accepted | a profile needs a fourth value in any of the three, which is a new rule and not a new label |
| D-263 | A correction never rewrites an accepted total. Corrections are append-only contributions with a direction of add or retract and an unsigned magnitude, because the quantity domain is unsigned and a negative integer has no encoding in it. Composition is the checked sum of additions minus the checked sum of retractions, retractions summing past the additions they correct reject rather than clamping to zero, and a period is rebuilt into a new generation that supersedes the previous one rather than edited. Clamping would produce a total that no sequence of contributions explains | accepted | a correction class appears that cannot be expressed as a directed contribution |
| D-264 | OpenTelemetry and ACP producers share one binding contract, packages/schemas/producer-accounting-binding-v1.schema.json, because the rule that governs both is the same rule: a generic integration is private analytics until an exact tuple is certified. The binding pins the producer's schema surface, instrumentation scope, metric or message shape, attribute disposition, accounting profile and certification state, and the schema itself makes effective_ceiling private-analytics for every certification state other than active. Two files restating that rule would be two authorities for it, which is the duplication SR-009 exists to remove. Every binding in conformance/accounting/producer-bindings-v1.json is below active today, so every one of them is private analytics | accepted | a producer kind appears whose certification rule genuinely differs, which would need its own ADR rather than a second copy of this one |
| D-265 | The source receipt is device-local and never crosses the device boundary. One receipt explains one NormalizedAccountingEvent: which observations saw the execution, which single one counted, why the others did not, under which profile, arithmetic and equivalence rule, and at what certification state. It records attestation as none with a basis of self-reported-at-source under D-100, so no receipt can assert that a provider verified a figure. Recording only the surviving observation would leave the receipt unable to explain why a discarded one was discarded, which is exactly what an appeal needs | accepted | a provider ships an individual-scope attestation path, which would give the receipt something other than a self-report to bind |
| D-266 | The evidence bundle is an at-rest device-local record and not a wire format. packages/schemas/evidence-bundle-v1.cddl binds the signed claim bytes by digest, the source receipt by identity and digest, the accounting profile and arithmetic by identity and digest, the provenance chain, the privacy decision and the observer-equivalence record. It adds no field to packages/schemas/egress-allowlist-v1.json and gains no COSE envelope: the only thing that crosses the boundary is the fixed-schema aggregate claim that already does. The bundle is what makes an accepted claim explainable later without making it transmissible | accepted | an appeal or export flow is shown to require server-side possession of a bundle, which would be a privacy decision and not an encoding one |
| D-267 | One appraisal record, packages/schemas/appraisal-result-v1.schema.json, replaces the three partial descriptions SR-017 cites. It carries the seven dimensions by name, the evaluated certification, rule bundle, accounting profile, equivalence rule and anomaly disposition beside them, and the canonical claim digest, evidence bundle digest, validity interval and supersession chain that no existing authority held. packages/schemas/appraisal-policy-v1.json binds each dimension name to the integer verifier-appraisal-v1 declares for it, so the CBOR appraisal and the JSON appraisal are one record rather than two with a matching field count. The three-state provenance, continuity and integrity columns in packages/schemas/planning-schema.sql are named as dropped_columns and every field the table cannot hold is named as unbound_fields, both checked against the DDL, so the remaining distance is machine-visible rather than prose | accepted | the SQL half lands, at which point the entries move from unbound_fields to bound_columns and the validator fails until they do |
| D-268 | The appraisal policy bundle binds packages/schemas/evidence-profile-policy-v1.json by path and content digest rather than copying its enums, so there is one dimension authority and a change to that file changes the bundle's identity. What the bundle adds is what evaluating a claim needs and that file does not carry: the wire ordinals, the validity interval, the verifier implementation digest, the supersession chain, the persistence binding, and the D-078 limb refinement that splits E1 into E1-S and E1-R with their availability. The refinement leaves the base enum untouched and is what the appraisal record and the wire use, which reconciles the machine-readable policy with the prose without forking it | accepted | the owning file adopts the limbs directly, at which point the refinement becomes an identity mapping and is removed |
| D-269 | The preimage of duplicate_domain_commitment is fixed by packages/schemas/observer-equivalence-v1.json and contains source-derived facts only. Every observer-derived input - collector artifact digest, collector instance, adapter instance, process identifier, device-local random, receive time, installation nonce - is forbidden, because including one guarantees that two observers of a single execution produce different commitments, which is how a client decided for itself what counted as a duplicate. Two observers that read the same source-assigned execution name collide and one counts. Two observers of one source runtime that agree on its cursor collide and one counts. Two collector instances in one mode that disagree quarantine the exclusivity unit rather than summing, because a wrong quarantine is recoverable and a double count is a scoring defect. Modes with different precedence resolve to the stronger and discard the weaker rather than retaining both. No new field crosses the device boundary; the constraint is on the preimage of a field already admissible in packages/schemas/egress-allowlist-v1.json | accepted | a source appears that exposes neither an execution name nor a stable cursor and must still compete, which would need a new equivalence class rather than a looser preimage |
| D-280 | The private-beta invite is a single-use code that binds exactly one account, and multi-use codes are rejected. A multi-use code is a shareable link: one forward turns invite-only into open registration silently, with no event anyone would notice, and the number of admissions stops being a quantity anybody can read. The owner's quota is therefore expressed as the number of codes issued rather than as a use count on one code, which makes the ring size equal to the number of rows in a table. The aggregate is the `invite-code` machine with states `issued`, `redeemed`, `expired`, `revoked` and `retired`, whose persistence owners are `invite_codes` and `invite_redemptions`, and it has no API state enum at all, because telling the invitee which of the four unredeemable conditions applied is exactly what makes enumeration productive | accepted | a use case appears that genuinely needs one code to admit a group, which would need its own decision about what the group is and how its size is bounded |
| D-281 | An invite code is 125 bits from a cryptographically secure random source, encoded in Crockford base 32 as 25 characters displayed in five groups of five, and it is stored only as SHA-256 over `'vibemaxxing-invite-v1'  concatenated with  0x00  concatenated with  canonical_code`. A leaked database yields 32-byte digests and no usable code. The digest is a plain hash rather than a memory-hard derivation, which is a decision rather than an omission: a memory-hard derivation defends a low-entropy secret against offline search, this secret carries 125 bits so offline search is not the threat, and a slow derivation would turn the redemption lookup from an index probe on a unique column into a scan over every live code with one derivation per row. The Crockford alphabet excludes `I`, `L`, `O` and `U`, and the server applies Crockford's confusable folding before hashing, so a transcription error in exactly the characters people confuse is accepted at no entropy cost | accepted | the code becomes short enough that offline search is credible, which would make a memory-hard derivation necessary and the lookup shape a separate problem |
| D-282 | Redemption atomicity is a database constraint and not worker discipline. `invite_redemptions.invite_code_id` is the primary key, so two concurrent redemptions of one code attempt two inserts of the same key and exactly one commits whatever isolation level either transaction ran at; `invite_redemptions.account_id` is unique, so one account cannot accumulate redemptions. The row lock taken while reading the code makes the loser fail early and cleanly, and the primary key is what makes it fail at all. The state column is a consequence rather than the control: check constraints bind `invite_codes.state` to the timestamp set, so a row cannot claim one lifecycle while recording another. A rule stated in prose is enforced by whoever read it; these two are enforced by the engine | accepted | a redemption path appears that cannot be expressed as one insert, which would need its own concurrency argument rather than an exception to this one |
| D-283 | The three admission gates are evaluated in a fixed order — the D-103 age floor before the authorization flow starts, the D-081 90-day provider-account gate at the OAuth callback, then invite redemption on an authenticated account — and the invite is deliberately last despite being the cheapest. A code presented before authentication makes the endpoint a free oracle whose response is a pure function of the code, with no account for a lockout to attach to; requiring authentication first prices every guess at one completed OAuth exchange against a provider account already 90 days old, so a guessing campaign costs a supply of aged provider accounts rather than a supply of HTTP requests. The order also fixes what each refusal leaks: the age floor surfaces no reason code at all because the flow never starts and nothing about the person reaches the server, `PROVIDER_ACCOUNT_TOO_NEW` is reachable by anyone with a provider account and so says nothing about the ring, `INVITE_REQUIRED` at 403 is reachable only by an authenticated principal and so is a statement to a person about their own account, and `INVITE_CODE_NOT_REDEEMABLE` at 422 covers unknown, expired, revoked and already-redeemed as one indistinguishable answer. `INVITE_ALREADY_REDEEMED` at 409 is the one deliberate distinction and is safe, because it reports a fact about the caller's own account that the caller can observe by being admitted | accepted | a measured false-refusal rate high enough that participants cannot tell a mistyped code from a spent one, which would trade the enumeration property for a support cost and needs the trade recorded rather than assumed |
| D-284 | The issuer is not disclosed to the invitee, and no invite chain exists or is retained. There is exactly one issuer today, so an issuer field would disclose nothing anybody does not already know — which is the argument against adding it, because a field whose privacy properties hold only while the population is one becomes wrong the day a second issuer exists. Participants cannot issue invites, so the graph is depth one and `invite_redemptions` holds it as a single row per admitted account with no ancestry and nothing accumulating across redemptions. That row is a social-graph edge held by the controller, is processed under Article 6(1)(a) consent as part of account creation rather than under Article 6(1)(f), because it exists to admit the participant rather than to secure the service and D-101 confines legitimate interests to security and fraud prevention, and is retained for the life of the account. `docs/privacy/DATA_MAP.md` carries both rows and remains the Article 30 record | accepted | participants gain the ability to issue invites, which creates a chain and reopens both the disclosure question and the retention window |
| D-285 | Invite issuance is an owner-only operator action through the operations tool and no administrative HTTP operation is added. `packages/schemas/openapi-v1.yaml` contains no administrative surface at all — every operation is public or authenticated as an ordinary participant, and there is no role, scope or moderator session anywhere in it — so an issuance endpoint would mean inventing an authorization model for a single route, and a model invented for one route is later reused by three. The owner is the only issuer and has direct database access under D-091. The outstanding-code quota, `invite_outstanding_max`, is enforced by the issuing tool and is explicitly not a database constraint, because PostgreSQL cannot express a bound on the number of rows in a state without a trigger or a counter table and that trigger would exist only to police the owner's own issuance rate. The asymmetry with D-282 is recorded rather than glossed: a mistake by the owner can exceed the quota and nothing stops it, which weakens the D-286 arithmetic in proportion and does not break it | accepted | a second issuer exists, which needs an authorization model and makes the endpoint worth building |
| D-286 | An invite proves that the owner chose to admit this account and proves nothing else. It is admission control, not proof of humanity, and not a Sybil control on its own: a person holding two codes holds two admitted accounts and nothing in the redemption path detects it, so the invite is a Sybil control exactly to the strength of the issuer's judgement about who receives a code, which is a human control producing no verifiable property. Under D-100 no provider offers individual-account attestation, so admission cannot be upgraded into evidence about the person behind it, and the technical load against duplicate identities stays where D-054, `docs/security/RANKED_IDENTITY_ELIGIBILITY.md` and ADR-020 already put it. Guessing is bounded first by 125 bits against at most 200 live codes, which is a hit probability near 5 × 10^-36 per guess; the `auth-start` class of `docs/architecture/API_EDGE_CONTRACT.md` at 10 per hour per address, chosen because the adversary is an address cycling through accounts rather than an account, and introducing no new number; and a per-account lockout after 5 consecutive failures for 24 hours, held as edge-side state beside the rate-limit buckets and deliberately not a persistence owner | accepted | a provider ships individual-account attestation, or measured abuse shows the ring is being farmed through issuance rather than through guessing, which is an issuance-discipline problem and not a protocol one |
| D-287 | Deleting or erasing an admitted account deletes the `invite_redemptions` row and moves the code from `redeemed` to the terminal `retired` in the same transaction. The edge disappears and the invite does not return to the pool. Deleting the redemption without retiring the code would make deletion a way to recycle an invite, by the participant or by anyone the code was shared with before it was spent; leaving the code in `redeemed` with no redemption row would make the state a lie, because `redeemed` means bound to exactly one account. The `invite_codes` row survives an erasure untouched because it names an issuer, five timestamps and a digest and names no invitee, which is why `packages/schemas/data-disposition-v1.json` classifies it non-personal with `retain-unlinked` while the redemption is personal with `delete`. Its retention is indefinite on purpose: it is the record that a 125-bit value has been used, and deleting it would let that value be issued a second time | accepted | a retention obligation that an indefinitely retained non-personal row cannot satisfy, which would need the re-issuance risk answered another way first |
| D-288 | An authenticated account with no redemption receives a session and reaches six operations: `redeemInvite`, `getMe`, `requestExport`, `requestDeletion`, `listSessions` and `revokeAllSessions`. Everything else answers 403 with `INVITE_REQUIRED`. The exemptions are not a convenience — Articles 15, 17 and 20 of Regulation (EU) 2016/679 do not wait on an admission decision, and taking a person's provider identity and then withholding the routes to see or erase it would create a data subject with no rights. `updateMe` is deliberately not exempt, which is the one place a rights argument is answered with a product one: the only rectifiable fields are the handle and the display name, the handle is a scarce competitive name held uniquely by `account_handles`, and admitting `updateMe` before redemption would let anyone who can complete an OAuth exchange reserve handles they will never compete under. `AccountProfile.admitted` is a boolean so a client renders the redemption screen without provoking a 403, and it is a boolean rather than a lifecycle because the account is told none of the five invite states | accepted | a supervisory authority, a court or engaged counsel holds that Article 16 rectification cannot be deferred past admission, which moves `updateMe` into the exempt set and needs the handle-reservation abuse answered separately |
| D-360 | The infrastructure spend ceiling is the measured steady-state monthly cost of the selected configuration at the modelled beta volume, not a fixed figure. This resolves D-094 by amending the ceiling, which is one of the three amendments ADR-017 step 4 permits; the scale target and the recovery objectives are unchanged. Provider credits fund the runway and do not enter the arithmetic: ADR-017 step 4 says selection does not proceed by counting credits, because a credit balance is a period of funding rather than a lower cost, and a ceiling satisfied by credits fails silently on the day they expire. | accepted | the measured steady-state cost exceeds what the owner will fund from revenue or personal spend, which reopens the scale target and the recovery objectives as the remaining amendable terms |
| D-361 | Amazon Web Services is the compute and database provider, in a European Union region, on the owner's existing account credit. AWS is the shortlist candidate ADR-017 records as clearing the residency and recovery gates most comfortably and as having the deepest managed-PostgreSQL recovery tooling, and it failed only the step 4 budget gate that D-360 has now amended. | accepted | a measured restore-elsewhere exercise fails, EU region availability or data processing terms change, or the steady-state cost after credits exceeds what D-360 allows |
| D-362 | AWS usage on the critical path is restricted to managed PostgreSQL, containers and object storage. ADR-017 records that this restriction keeps the exit cost comparable to a smaller provider's and that every additional managed service moves it, so the restriction is what makes D-361 compatible with the portability criterion rather than an aspiration alongside it. Adopting any further AWS managed service on the critical path requires an amendment naming the service, its portability cost and its exit path. | accepted | a required capability has no implementation within the three permitted services, which is an amendment rather than an exception |
| D-363 | The credit balance is runway with a cliff, and the cliff is recorded rather than discovered. Before the credits are consumed the steady-state cost is measured against D-360 and either funded, reduced, or met by amending the scale target or the recovery objectives under ADR-017's rule. A credit balance is not evidence that the configuration is affordable and is never cited as satisfying a budget gate. | accepted | the credits are exhausted or expire, at which point the measurement is due rather than optional |
